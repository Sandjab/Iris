# Phase 6.3b — Onglet Settings (UI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter le 6e onglet « Settings » au popover menu-bar : affichage des réglages (`config.get`), édition immédiate des champs *hot* (`config.set`), gestion CA (statut + install/uninstall), Reveal/Reload du `config.json`.

**Architecture:** L'UI consomme le backend 6.3a déjà mergé. Un seul ajout daemon : le RPC `config.path` (le chemin du fichier n'est exposé nulle part). Toute la logique testable vit dans `IrisAppCore` (`AppModel` + un seam `CATrustInstalling`) ; la vue SwiftUI (`Form`/`GroupBox`) est smoke-only. Les mutations suivent le pattern existant « RPC puis re-fetch » (Phase 6.2).

**Tech Stack:** Swift 5.9+, SwiftUI/AppKit, swift-nio (RPC), XCTest. Cibles : `IrisKit` (backend), `IrisAppCore` (logique testée), `IrisApp` (vue, gate CI xcodebuild macos-15).

**Spec:** `docs/superpowers/specs/2026-06-05-phase-6.3b-settings-ui-design.md`

**Conventions de référence (à imiter) :**
- Conformer RPC : `Sources/IrisAppCore/Protocols/IrisKitConformances.swift` (ex. `addSecret`).
- Mutations AppModel `via admin:` + re-fetch : `Sources/IrisAppCore/AppModel.swift:134-176`.
- Mock : `Tests/IrisAppCoreTests/Mocks/FakeAdminCalling.swift`.
- Tests CRUD : `Tests/IrisAppCoreTests/AppModelCRUDTests.swift`.
- Vue onglet : `IrisApp/IrisApp/SecretsTab.swift` (imports, `@EnvironmentObject`, `let admin: AdminCalling`).
- Idempotence CA + flux : `Sources/iris/Commands/CACommands.swift:102-153`.

**Commandes globales :**
- Build : `swift build`
- Tests : `swift test` (cœur + backend) ; un test isolé : `swift test --filter <Suite>/<test>`
- Lint : `swift-format lint --strict --recursive Sources Tests`
- Format en place : `swift-format format -i -r Sources Tests`

---

## Task 1 : Backend — RPC `config.path` (IrisKit)

**Files:**
- Modify: `Sources/IrisKit/Config/ConfigStore.swift` (ajout accesseur `filePath`, après `current` ~l.44)
- Modify: `Sources/IrisKit/IPC/AdminProtocol.swift` (case `configPath` ~l.23 ; struct `ConfigPathResult` dans la section Results)
- Modify: `Sources/IrisKit/IPC/AdminDispatcher.swift` (case `.configPath` après `.configGet` ~l.216)
- Test: `Tests/IrisKitTests/Config/ConfigStoreTests.swift`, `Tests/IrisKitTests/AdminDispatcherTests.swift`

- [ ] **Step 1 : Test échouant — `ConfigStore.filePath`**

Ajouter à `ConfigStoreTests` :

```swift
func testFilePathReturnsResolvedPath() async throws {
    let store = try ConfigStore(path: path, logger: logger)
    let fp = await store.filePath
    XCTAssertEqual(fp, path.path)
}
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `swift test --filter ConfigStoreTests/testFilePathReturnsResolvedPath`
Expected: FAIL — `value of type 'ConfigStore' has no member 'filePath'` (échec de compilation).

- [ ] **Step 3 : Implémenter `filePath`**

Dans `ConfigStore.swift`, juste après `public var current: Config { config }` (~l.44) :

```swift
    /// Resolved path of the config file on disk (for the app's "Reveal in Finder").
    public var filePath: String { path.path }
```

- [ ] **Step 4 : Vérifier le passage**

Run: `swift test --filter ConfigStoreTests/testFilePathReturnsResolvedPath`
Expected: PASS.

- [ ] **Step 5 : Ajouter le verbe RPC + le type résultat**

Dans `AdminProtocol.swift`, ajouter le case après `case configSet = "config.set"` (l.23) :

```swift
    case configPath = "config.path"
```

Dans la section `// MARK: - Results` (après `ConfigSetResult`, ~l.232) :

```swift
public struct ConfigPathResult: Codable, Sendable, Equatable {
    public let path: String
    public init(path: String) { self.path = path }
}
```

- [ ] **Step 6 : Router dans le dispatcher**

Dans `AdminDispatcher.swift`, juste après le bloc `case .configGet:` (qui fait `return try JSONValue.encoding(await configStore.current)`, ~l.215-216) :

```swift
        case .configPath:
            return try JSONValue.encoding(ConfigPathResult(path: await configStore.filePath))
```

- [ ] **Step 7 : Test échouant — routing dispatcher**

Ajouter à `AdminDispatcherTests` (imiter le test `.caExportPath`, ~l.323) :

```swift
func testConfigPathReturnsConfigFilePath() async throws {
    let (dispatcher, _, _) = try await makeDispatcher()
    let resp = await dispatcher.dispatch(request(.configPath))
    let result = try unwrapResult(resp).decode(as: ConfigPathResult.self)
    XCTAssertFalse(result.path.isEmpty)
    XCTAssertTrue(result.path.hasSuffix(".json"), "got \(result.path)")
}
```

- [ ] **Step 8 : Vérifier le passage de toute la suite IrisKit**

Run: `swift test --filter IrisKitTests`
Expected: PASS (dont `testConfigPathReturnsConfigFilePath` et `testFilePathReturnsResolvedPath`).

- [ ] **Step 9 : Format + commit**

```bash
swift-format format -i -r Sources Tests
swift-format lint --strict --recursive Sources Tests
git add Sources/IrisKit Tests/IrisKitTests
git commit -m "feat(phase-6.3b): RPC config.path expose le chemin du config.json"
```

---

## Task 2 : Core — étendre `AdminCalling` (protocole + conformer + mock)

L'ajout de méthodes au protocole `AdminCalling` casse atomiquement tous les conformers ; protocole + `AdminClient` + `FakeAdminCalling` doivent atterrir ensemble.

**Files:**
- Modify: `Sources/IrisAppCore/Protocols/AdminCalling.swift` (6 méthodes)
- Modify: `Sources/IrisAppCore/Protocols/IrisKitConformances.swift` (conformance `AdminClient`)
- Modify: `Tests/IrisAppCoreTests/Mocks/FakeAdminCalling.swift` (stubs + champs)
- Test: `Tests/IrisAppCoreTests/AppModelConfigTests.swift` (nouveau)

- [ ] **Step 1 : Test échouant — le mock répond aux nouveaux appels**

Créer `Tests/IrisAppCoreTests/AppModelConfigTests.swift` :

```swift
import IrisKit
import XCTest

@testable import IrisAppCore

@MainActor
final class AppModelConfigTests: XCTestCase {
    func testFakeRecordsAndReturnsConfigStubs() async throws {
        let admin = FakeAdminCalling()
        admin.stubConfig = .default
        admin.stubCATrusted = true
        admin.stubConfigPath = "/tmp/iris/config.json"
        admin.stubCAExportPath = "/tmp/iris/ca.pem"

        _ = try await admin.fetchConfig()
        _ = try await admin.setConfig(updates: [ConfigSetParams.Update(key: "backups.max_count", value: "3")])
        _ = try await admin.reloadConfig()
        let cfgPath = try await admin.configPath()
        let trusted = try await admin.isCATrusted()
        let caPath = try await admin.caExportPath()

        XCTAssertEqual(cfgPath, "/tmp/iris/config.json")
        XCTAssertTrue(trusted)
        XCTAssertEqual(caPath, "/tmp/iris/ca.pem")
        XCTAssertEqual(
            admin.calls,
            ["fetchConfig", "setConfig(backups.max_count=3)", "reloadConfig", "configPath", "isCATrusted", "caExportPath"]
        )
    }
}
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `swift test --filter AppModelConfigTests/testFakeRecordsAndReturnsConfigStubs`
Expected: FAIL — méthodes/membres inconnus (échec de compilation).

- [ ] **Step 3 : Étendre le protocole**

Dans `AdminCalling.swift`, avant la `}` finale du protocole (après `deleteRule`, l.47) :

```swift

    /// Fetch the full config snapshot (broker/security/backups/hosts).
    func fetchConfig() async throws -> Config

    /// Apply scalar config updates; returns applied vs restart-required keys.
    func setConfig(updates: [ConfigSetParams.Update]) async throws -> ConfigSetResult

    /// Re-read config.json from disk and re-apply.
    func reloadConfig() async throws -> ConfigReloadResult

    /// Resolved path of config.json on disk (for Reveal in Finder).
    func configPath() async throws -> String

    /// Whether the IRIS CA is in the user trust store.
    func isCATrusted() async throws -> Bool

    /// Path of the public CA PEM (for install/uninstall).
    func caExportPath() async throws -> String
```

- [ ] **Step 4 : Conformer `AdminClient`**

Dans `IrisKitConformances.swift`, avant la `}` de `extension AdminClient: AdminCalling` (après `deleteRule`, l.89) :

```swift

    public func fetchConfig() async throws -> Config {
        try await call(.configGet, returning: Config.self)
    }

    public func setConfig(updates: [ConfigSetParams.Update]) async throws -> ConfigSetResult {
        try await call(.configSet, params: ConfigSetParams(updates: updates), returning: ConfigSetResult.self)
    }

    public func reloadConfig() async throws -> ConfigReloadResult {
        try await call(.configReload, returning: ConfigReloadResult.self)
    }

    public func configPath() async throws -> String {
        try await call(.configPath, returning: ConfigPathResult.self).path
    }

    public func isCATrusted() async throws -> Bool {
        try await call(.caIsTrusted, returning: CAIsTrustedResult.self).trusted
    }

    public func caExportPath() async throws -> String {
        try await call(.caExportPath, returning: CAExportPathResult.self).path
    }
```

- [ ] **Step 5 : Étendre le mock**

Dans `FakeAdminCalling.swift`, ajouter les champs stub après `var stubRules` (l.18) :

```swift
    var stubConfig: Config = .default
    var stubCATrusted: Bool = false
    var stubConfigPath: String = "/tmp/iris/config.json"
    var stubCAExportPath: String = "/tmp/iris/ca.pem"
    var stubSetResult: ConfigSetResult = ConfigSetResult(applied: [], requiresRestart: [])
    var stubReloadResult: ConfigReloadResult = ConfigReloadResult(reloaded: true, ignored: [])
```

Et les méthodes avant la `}` finale (après `deleteRule`, l.122) :

```swift

    func fetchConfig() async throws -> Config {
        calls.append("fetchConfig")
        if let e = shouldThrow { throw e }
        return stubConfig
    }

    func setConfig(updates: [ConfigSetParams.Update]) async throws -> ConfigSetResult {
        calls.append("setConfig(\(updates.map { "\($0.key)=\($0.value)" }.joined(separator: ",")))")
        if let e = shouldThrow { throw e }
        return stubSetResult
    }

    func reloadConfig() async throws -> ConfigReloadResult {
        calls.append("reloadConfig")
        if let e = shouldThrow { throw e }
        return stubReloadResult
    }

    func configPath() async throws -> String {
        calls.append("configPath")
        if let e = shouldThrow { throw e }
        return stubConfigPath
    }

    func isCATrusted() async throws -> Bool {
        calls.append("isCATrusted")
        if let e = shouldThrow { throw e }
        return stubCATrusted
    }

    func caExportPath() async throws -> String {
        calls.append("caExportPath")
        if let e = shouldThrow { throw e }
        return stubCAExportPath
    }
```

- [ ] **Step 6 : Vérifier le passage**

Run: `swift test --filter AppModelConfigTests/testFakeRecordsAndReturnsConfigStubs`
Expected: PASS.

- [ ] **Step 7 : Format + commit**

```bash
swift-format format -i -r Sources Tests
swift-format lint --strict --recursive Sources Tests
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.3b): AdminCalling — config + CA (protocole, conformer, mock)"
```

---

## Task 3 : `AppModel.loadConfig` + propriété `config`

**Files:**
- Modify: `Sources/IrisAppCore/AppModel.swift`
- Test: `Tests/IrisAppCoreTests/AppModelConfigTests.swift`

- [ ] **Step 1 : Test échouant**

Ajouter à `AppModelConfigTests` :

```swift
func testLoadConfigFetchesAndStores() async throws {
    let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
    let admin = FakeAdminCalling()
    admin.stubConfig = .default
    try await model.loadConfig(via: admin)
    XCTAssertEqual(admin.calls, ["fetchConfig"])
    XCTAssertEqual(model.config?.version, 1)
    XCTAssertEqual(model.config?.security.onExfilAttempt, .blockAndNotify)
}
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `swift test --filter AppModelConfigTests/testLoadConfigFetchesAndStores`
Expected: FAIL — `config` et `loadConfig` inconnus.

- [ ] **Step 3 : Implémenter**

Dans `AppModel.swift`, ajouter la propriété publiée après `@Published public var rules` (l.15) :

```swift
    @Published public var config: Config?
    @Published public var caTrusted: Bool?
```

Et la méthode dans la section CRUD (après `deleteRule`, l.176, avant la `}` de classe) :

```swift
    // MARK: - Config / CA (Phase 6.3b)

    public func loadConfig(via admin: AdminCalling) async throws {
        config = try await admin.fetchConfig()
    }
```

- [ ] **Step 4 : Vérifier le passage**

Run: `swift test --filter AppModelConfigTests/testLoadConfigFetchesAndStores`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
swift-format format -i -r Sources Tests
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.3b): AppModel.loadConfig + état config/caTrusted"
```

---

## Task 4 : `AppModel.setConfig` (RPC puis re-fetch)

**Files:**
- Modify: `Sources/IrisAppCore/AppModel.swift`
- Test: `Tests/IrisAppCoreTests/AppModelConfigTests.swift`

- [ ] **Step 1 : Test échouant**

```swift
func testSetConfigCallsRPCThenReloads() async throws {
    let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
    let admin = FakeAdminCalling()
    admin.stubConfig = .default
    admin.stubSetResult = ConfigSetResult(applied: ["security.on_exfil_attempt"], requiresRestart: [])
    let result = try await model.setConfig(
        [ConfigSetParams.Update(key: "security.on_exfil_attempt", value: "block_only")],
        via: admin
    )
    XCTAssertEqual(result.applied, ["security.on_exfil_attempt"])
    XCTAssertEqual(admin.calls, ["setConfig(security.on_exfil_attempt=block_only)", "fetchConfig"])
}
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `swift test --filter AppModelConfigTests/testSetConfigCallsRPCThenReloads`
Expected: FAIL — `setConfig` inconnu.

- [ ] **Step 3 : Implémenter** (après `loadConfig`) :

```swift
    @discardableResult
    public func setConfig(
        _ updates: [ConfigSetParams.Update],
        via admin: AdminCalling
    ) async throws -> ConfigSetResult {
        let result = try await admin.setConfig(updates: updates)
        try await loadConfig(via: admin)
        return result
    }
```

- [ ] **Step 4 : Vérifier le passage**

Run: `swift test --filter AppModelConfigTests/testSetConfigCallsRPCThenReloads`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
swift-format format -i -r Sources Tests
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.3b): AppModel.setConfig RPC puis re-fetch"
```

---

## Task 5 : `AppModel.refreshCATrust` + `reloadConfig` + `configFilePath`

**Files:**
- Modify: `Sources/IrisAppCore/AppModel.swift`
- Test: `Tests/IrisAppCoreTests/AppModelConfigTests.swift`

- [ ] **Step 1 : Test échouant**

```swift
func testRefreshCATrustReloadAndConfigPath() async throws {
    let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
    let admin = FakeAdminCalling()
    admin.stubConfig = .default
    admin.stubCATrusted = true
    admin.stubConfigPath = "/tmp/iris/config.json"

    try await model.refreshCATrust(via: admin)
    XCTAssertEqual(model.caTrusted, true)

    try await model.reloadConfig(via: admin)
    let path = try await model.configFilePath(via: admin)
    XCTAssertEqual(path, "/tmp/iris/config.json")
    XCTAssertEqual(admin.calls, ["isCATrusted", "reloadConfig", "fetchConfig", "configPath"])
}
```

- [ ] **Step 2 : Vérifier l'échec**

Run: `swift test --filter AppModelConfigTests/testRefreshCATrustReloadAndConfigPath`
Expected: FAIL — méthodes inconnues.

- [ ] **Step 3 : Implémenter** (après `setConfig`) :

```swift
    public func refreshCATrust(via admin: AdminCalling) async throws {
        caTrusted = try await admin.isCATrusted()
    }

    public func reloadConfig(via admin: AdminCalling) async throws {
        _ = try await admin.reloadConfig()
        try await loadConfig(via: admin)
    }

    public func configFilePath(via admin: AdminCalling) async throws -> String {
        try await admin.configPath()
    }
```

- [ ] **Step 4 : Vérifier le passage**

Run: `swift test --filter AppModelConfigTests/testRefreshCATrustReloadAndConfigPath`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
swift-format format -i -r Sources Tests
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.3b): AppModel refreshCATrust/reloadConfig/configFilePath"
```

---

## Task 6 : Seam CA install + `AppModel.installCA/uninstallCA`

**Files:**
- Create: `Sources/IrisAppCore/CATrustInstalling.swift`
- Modify: `Sources/IrisAppCore/AppModel.swift` (param `caInstaller`, méthodes install/uninstall)
- Create: `Tests/IrisAppCoreTests/Mocks/FakeCATrustInstaller.swift`
- Test: `Tests/IrisAppCoreTests/AppModelConfigTests.swift`

- [ ] **Step 1 : Créer le protocole + l'impl système**

`Sources/IrisAppCore/CATrustInstalling.swift` :

```swift
import IrisKit

/// Seam over the (GUI-blocking, non-testable) trust-store mutation so AppModel's
/// install/uninstall orchestration is unit-testable with a fake.
public protocol CATrustInstalling: Sendable {
    func install(pemPath: String) throws
    func uninstall(pemPath: String) throws
}

/// Production impl: shells out via IrisKit's `CATrustStore` (`/usr/bin/security`,
/// presents the auth panel). Smoke-tested, not unit-tested.
public struct SystemCATrustInstaller: CATrustInstalling {
    public init() {}
    public func install(pemPath: String) throws { try CATrustStore.install(pemPath: pemPath) }
    public func uninstall(pemPath: String) throws { try CATrustStore.uninstall(pemPath: pemPath) }
}
```

- [ ] **Step 2 : Créer le fake installer**

`Tests/IrisAppCoreTests/Mocks/FakeCATrustInstaller.swift` :

```swift
import Foundation

@testable import IrisAppCore

final class FakeCATrustInstaller: CATrustInstalling, @unchecked Sendable {
    private let lock = NSLock()
    private(set) var installedPath: String?
    private(set) var uninstalledPath: String?
    var shouldThrow: Error?

    func install(pemPath: String) throws {
        if let e = shouldThrow { throw e }
        lock.lock(); installedPath = pemPath; lock.unlock()
    }

    func uninstall(pemPath: String) throws {
        if let e = shouldThrow { throw e }
        lock.lock(); uninstalledPath = pemPath; lock.unlock()
    }
}
```

- [ ] **Step 3 : Test échouant — installCA**

```swift
func testInstallCAExportsPathThenInstallsAndRefreshes() async throws {
    let admin = FakeAdminCalling()
    admin.stubCAExportPath = "/tmp/iris/ca.pem"
    admin.stubCATrusted = true  // post-install refresh sees trusted
    let installer = FakeCATrustInstaller()
    let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!, caInstaller: installer)
    model.caTrusted = false  // not yet trusted → install proceeds

    try await model.installCA(via: admin)

    XCTAssertEqual(installer.installedPath, "/tmp/iris/ca.pem")
    XCTAssertEqual(admin.calls, ["caExportPath", "isCATrusted"])
    XCTAssertEqual(model.caTrusted, true)
}

func testInstallCANoopWhenAlreadyTrusted() async throws {
    let admin = FakeAdminCalling()
    let installer = FakeCATrustInstaller()
    let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!, caInstaller: installer)
    model.caTrusted = true
    try await model.installCA(via: admin)
    XCTAssertNil(installer.installedPath)
    XCTAssertEqual(admin.calls, [])  // idempotent: no RPC, no shell-out
}

func testUninstallCAExportsPathThenUninstallsAndRefreshes() async throws {
    let admin = FakeAdminCalling()
    admin.stubCAExportPath = "/tmp/iris/ca.pem"
    admin.stubCATrusted = false
    let installer = FakeCATrustInstaller()
    let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!, caInstaller: installer)
    model.caTrusted = true
    try await model.uninstallCA(via: admin)
    XCTAssertEqual(installer.uninstalledPath, "/tmp/iris/ca.pem")
    XCTAssertEqual(admin.calls, ["caExportPath", "isCATrusted"])
    XCTAssertEqual(model.caTrusted, false)
}
```

- [ ] **Step 4 : Vérifier l'échec**

Run: `swift test --filter AppModelConfigTests/testInstallCAExportsPathThenInstallsAndRefreshes`
Expected: FAIL — `caInstaller` param et `installCA` inconnus.

- [ ] **Step 5 : Implémenter le param `caInstaller` + les méthodes**

Dans `AppModel.swift`, ajouter le stockage après `private let defaults: UserDefaults` (l.29) :

```swift
    private let caInstaller: CATrustInstalling
```

Modifier l'`init` (l.38) en ajoutant le paramètre défaillé (non-cassant) :

```swift
    public init(defaults: UserDefaults = .standard, caInstaller: CATrustInstalling = SystemCATrustInstaller()) {
        self.defaults = defaults
        self.caInstaller = caInstaller
        let storedTab = defaults.string(forKey: Self.tabKey).flatMap(Tab.init(rawValue:))
        self.selectedTab = storedTab ?? .overview
        if let ts = defaults.object(forKey: Self.ackKey) as? Date {
            self.lastAcknowledgedAt = ts
        }
    }
```

Ajouter les méthodes (après `configFilePath`). Le shell-out bloque sur le panneau d'auth → l'exécuter **hors du main actor** via `Task.detached` pour ne pas geler le popover :

```swift
    public func installCA(via admin: AdminCalling) async throws {
        if caTrusted == true { return }  // idempotent: skip the auth prompt
        let path = try await admin.caExportPath()
        let installer = caInstaller
        try await Task.detached { try installer.install(pemPath: path) }.value
        try await refreshCATrust(via: admin)
    }

    public func uninstallCA(via admin: AdminCalling) async throws {
        if caTrusted == false { return }
        let path = try await admin.caExportPath()
        let installer = caInstaller
        try await Task.detached { try installer.uninstall(pemPath: path) }.value
        try await refreshCATrust(via: admin)
    }
```

- [ ] **Step 6 : Vérifier le passage des 3 tests CA**

Run: `swift test --filter AppModelConfigTests`
Expected: PASS (install, no-op, uninstall + les tests précédents).

- [ ] **Step 7 : Format + commit**

```bash
swift-format format -i -r Sources Tests
swift-format lint --strict --recursive Sources Tests
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.3b): seam CATrustInstalling + AppModel install/uninstall CA"
```

---

## Task 7 : Bootstrap — charger config + statut CA

**Files:**
- Modify: `Sources/IrisAppCore/SyncCoordinator.swift:28-47` (`bootstrap`)
- Test: `Tests/IrisAppCoreTests/SyncCoordinatorTests.swift:20-55` (assertion `calls`)

- [ ] **Step 1 : Mettre à jour le test (le faire échouer d'abord)**

Dans `SyncCoordinatorTests.swift`, à `testBootstrapWithDaemonUpFetchesStatusAndEvents` (l.43-46), remplacer l'assertion `calls` par :

```swift
        XCTAssertEqual(
            admin.calls,
            ["status", "queryEvents(since:-1.0,limit:100)", "listSecrets", "listRules", "fetchConfig", "isCATrusted"]
        )
```

Ajouter en fin de test (avant la `}`), pour verrouiller le peuplement :

```swift
        XCTAssertEqual(model.config?.version, 1)
        XCTAssertNotNil(model.caTrusted)
```

(Le fake renvoie `stubConfig = .default` et `stubCATrusted = false` par défaut.)

- [ ] **Step 2 : Vérifier l'échec**

Run: `swift test --filter SyncCoordinatorTests/testBootstrapWithDaemonUpFetchesStatusAndEvents`
Expected: FAIL — `calls` ne contient pas encore `fetchConfig`/`isCATrusted`, `model.config` est nil.

- [ ] **Step 3 : Étendre `bootstrap`**

Dans `SyncCoordinator.bootstrap()`, ajouter les deux fetches après `let rules = try await admin.listRules()` (l.33), et l'affectation après `model.rules = ...` (l.41) :

```swift
            let rules = try await admin.listRules()
            let config = try await admin.fetchConfig()
            let caTrusted = try await admin.isCATrusted()
            model.daemonStatus = .up(
                stats: status.stats,
                uptime: TimeInterval(status.uptimeS),
                paused: false  // SPECS §15.4 — pause state isn't in daemon.status; infer false on bootstrap
            )
            model.ingestBatch(recent.reversed())
            model.secrets = secrets.sorted { $0.name < $1.name }
            model.rules = rules.sorted { $0.host < $1.host }
            model.config = config
            model.caTrusted = caTrusted
```

(Remplacer le bloc d'affectations existant l.34-41 par celui-ci ; le `catch` reste inchangé.)

- [ ] **Step 4 : Vérifier le passage de toute la suite IrisAppCore**

Run: `swift test --filter IrisAppCoreTests`
Expected: PASS (bootstrap mis à jour + aucun autre test cassé).

- [ ] **Step 5 : Commit**

```bash
swift-format format -i -r Sources Tests
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.3b): bootstrap charge config + statut CA"
```

---

## Task 8 : Onglet `.settings` (enum Tab + PopoverView + TabBar)

**Files:**
- Modify: `Sources/IrisAppCore/AppModel.swift:7-9` (enum `Tab`)
- Modify: `IrisApp/IrisApp/PopoverView.swift:21-27` (switch) et `:147-153` (TabBar items)

- [ ] **Step 1 : Vérifier qu'aucun test ne fige le nombre d'onglets**

Run: `grep -rn "Tab.allCases" Tests/ Sources/`
Expected: aucun résultat de comptage à mettre à jour (sinon ajuster `count` à 6). Si une assertion existe, la corriger maintenant.

- [ ] **Step 2 : Ajouter le case à l'enum**

Dans `AppModel.swift`, l.8 :

```swift
        case overview, logs, security, secrets, rules, settings
```

- [ ] **Step 3 : Router dans PopoverView**

Dans `PopoverView.swift`, dans le `switch model.selectedTab` (l.21-27), ajouter avant la `}` :

```swift
                case .settings: SettingsTab(admin: admin)
```

- [ ] **Step 4 : Ajouter l'item TabBar**

Dans `TabBar.items` (`PopoverView.swift:147-153`), ajouter après la ligne `rules` :

```swift
        Item(tab: .settings, title: "Settings", symbol: "gearshape"),
```

- [ ] **Step 5 : Compiler le coeur** (la vue arrive Task 9 — si elle n'existe pas encore, ce step échouera à la compilation Xcode ; faire Task 9 d'abord ou en parallèle puis recompiler).

Run: `swift build`
Expected: `swift build` (cible IrisAppCore + irisd + iris) reste vert ; l'enum compile. `SettingsTab` est dans la cible Xcode `IrisApp` (hors `swift build`) → ce step ne couvre PAS la vue ; sa compilation est validée par le gate CI xcodebuild (Task 10).

- [ ] **Step 6 : Commit** (groupé avec Task 9 si fait en parallèle)

```bash
swift-format format -i -r Sources
git add Sources/IrisAppCore IrisApp/IrisApp/PopoverView.swift
git commit -m "feat(phase-6.3b): onglet Settings (Tab + PopoverView + TabBar)"
```

---

## Task 9 : Vue `SettingsTab.swift` (smoke-only)

**Files:**
- Create: `IrisApp/IrisApp/SettingsTab.swift`

> Pas de test unitaire (vue SwiftUI + panneau d'auth = smoke poste). La compilation est jugée par le **gate CI xcodebuild macos-15** (le build local n'est pas l'oracle — leçon durable). Agencement C : `Form`/`GroupBox`. Apply au commit (D1) : `Picker` applique `onChange` ; champs numériques appliquent sur `onSubmit`/perte de focus, jamais par incrément.

- [ ] **Step 1 : Créer la vue**

`IrisApp/IrisApp/SettingsTab.swift` :

```swift
import AppKit
import IrisAppCore
import IrisKit
import SwiftUI

struct SettingsTab: View {
    @EnvironmentObject var model: AppModel
    let admin: AdminCalling

    @State private var maxSubsText: String = ""
    @State private var maxBackupsText: String = ""
    @State private var errorText: String?
    @State private var statusText: String?

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 12) {
                if let cfg = model.config {
                    securityBox(cfg)
                    backupsBox()
                    caBox()
                    connectionBox(cfg)
                    footer()
                } else {
                    ProgressView().frame(maxWidth: .infinity, minHeight: 200)
                }
                if let errorText {
                    Text(errorText).foregroundStyle(.red).font(.caption)
                }
                if let statusText {
                    Text(statusText).foregroundStyle(.secondary).font(.caption)
                }
            }
            .padding(12)
        }
        .task { await reload() }
    }

    // MARK: - Sections

    @ViewBuilder private func securityBox(_ cfg: Config) -> some View {
        GroupBox("Security") {
            VStack(alignment: .leading, spacing: 8) {
                HStack {
                    Text("On exfil attempt")
                    Spacer()
                    Picker(
                        "",
                        selection: Binding(
                            get: { cfg.security.onExfilAttempt },
                            set: { apply(key: "security.on_exfil_attempt", value: $0.rawValue) }
                        )
                    ) {
                        ForEach(ExfilAttemptPolicy.allCases, id: \.self) { policy in
                            Text(policy.rawValue).tag(policy)
                        }
                    }
                    .labelsHidden()
                    .frame(width: 190)
                }
                HStack {
                    Text("Max substitutions / min")
                    Spacer()
                    TextField("", text: $maxSubsText)
                        .frame(width: 70)
                        .multilineTextAlignment(.trailing)
                        .onSubmit { apply(key: "security.max_substitutions_per_minute", value: maxSubsText) }
                }
            }
            .frame(maxWidth: .infinity, alignment: .leading)
            .padding(4)
        }
    }

    @ViewBuilder private func backupsBox() -> some View {
        GroupBox("Backups") {
            HStack {
                Text("Keep backups")
                Spacer()
                TextField("", text: $maxBackupsText)
                    .frame(width: 70)
                    .multilineTextAlignment(.trailing)
                    .onSubmit { apply(key: "backups.max_count", value: maxBackupsText) }
            }
            .frame(maxWidth: .infinity, alignment: .leading)
            .padding(4)
        }
    }

    @ViewBuilder private func caBox() -> some View {
        GroupBox("Certificate Authority") {
            HStack {
                switch model.caTrusted {
                case .some(true):
                    Label("Trusted", systemImage: "checkmark.seal.fill").foregroundStyle(.green)
                case .some(false):
                    Label("Not trusted", systemImage: "xmark.seal").foregroundStyle(.orange)
                case nil:
                    Text("Unknown").foregroundStyle(.secondary)
                }
                Spacer()
                if model.caTrusted == true {
                    Button("Uninstall…") { caAction(install: false) }
                } else {
                    Button("Install…") { caAction(install: true) }
                }
            }
            .frame(maxWidth: .infinity, alignment: .leading)
            .padding(4)
        }
    }

    @ViewBuilder private func connectionBox(_ cfg: Config) -> some View {
        GroupBox("Connection (read-only)") {
            VStack(alignment: .leading, spacing: 4) {
                roRow("Proxy", cfg.broker.listen)
                roRow("Events", cfg.broker.eventsListen)
                roRow("Admin socket", cfg.broker.adminSocket)
                roRow("Log level", cfg.broker.logLevel.rawValue)
                roRow("Event retention", "\(cfg.broker.eventRetentionDays) days")
                roRow("Event ring size", "\(cfg.broker.eventRingSize)")
            }
            .frame(maxWidth: .infinity, alignment: .leading)
            .padding(4)
        }
    }

    @ViewBuilder private func footer() -> some View {
        HStack {
            Button("Reveal config.json") { reveal() }
            Button("Reload") {
                Task {
                    errorText = nil
                    do { try await model.reloadConfig(via: admin); syncFields() }
                    catch { errorText = userMessage(error) }
                }
            }
            Spacer()
        }
    }

    private func roRow(_ key: String, _ value: String) -> some View {
        HStack {
            Text(key).foregroundStyle(.secondary)
            Spacer()
            Text(value).foregroundStyle(.secondary).textSelection(.enabled)
        }
    }

    // MARK: - Actions

    private func apply(key: String, value: String) {
        Task {
            errorText = nil
            statusText = nil
            do {
                _ = try await model.setConfig([ConfigSetParams.Update(key: key, value: value)], via: admin)
                statusText = "Applied."
                syncFields()
            } catch {
                errorText = userMessage(error)
                syncFields()  // revert displayed value to the daemon's truth
            }
        }
    }

    private func caAction(install: Bool) {
        Task {
            errorText = nil
            do {
                if install { try await model.installCA(via: admin) } else { try await model.uninstallCA(via: admin) }
            } catch {
                errorText = userMessage(error)
            }
        }
    }

    private func reveal() {
        Task {
            do {
                let path = try await model.configFilePath(via: admin)
                NSWorkspace.shared.activateFileViewerSelecting([URL(fileURLWithPath: path)])
            } catch {
                errorText = userMessage(error)
            }
        }
    }

    private func reload() async {
        errorText = nil
        do {
            try await model.loadConfig(via: admin)
            try await model.refreshCATrust(via: admin)
            syncFields()
        } catch {
            errorText = userMessage(error)
        }
    }

    private func syncFields() {
        guard let cfg = model.config else { return }
        maxSubsText = "\(cfg.security.maxSubstitutionsPerMinute)"
        maxBackupsText = "\(cfg.backups.maxCount)"
    }
}
```

- [ ] **Step 2 : Ajouter le fichier à la cible Xcode**

Le projet ajoute les `.swift` au target via le système de fichiers Xcode. Vérifier que `SettingsTab.swift` est membre de la cible `IrisApp` (sinon l'ajouter dans Xcode → File Inspector → Target Membership). Les autres `*Tab.swift` servent de modèle.

- [ ] **Step 3 : Build local de la vue (indicatif, pas l'oracle)**

Run: `xcodebuild -scheme IrisApp -configuration Debug build 2>&1 | tail -5`
Expected: `** BUILD SUCCEEDED **` (le verdict ferme = CI macos-15, Task 10).

- [ ] **Step 4 : Commit** (avec Task 8 si non encore commité)

```bash
git add IrisApp/IrisApp/SettingsTab.swift IrisApp/IrisApp.xcodeproj
git commit -m "feat(phase-6.3b): vue SettingsTab (Form/GroupBox, CA, reveal/reload)"
```

---

## Task 10 : Intégration — build, tests, lint, push, CI, smoke

**Files:** aucun (vérification + PR).

- [ ] **Step 1 : Suite complète + lint verts**

```bash
swift build
swift test
swift-format lint --strict --recursive Sources Tests
```
Expected: build clean ; tous les tests PASS (baseline 447 + nouveaux) ; lint clean.

- [ ] **Step 2 : Push de la branche**

```bash
git push -u origin feat/phase-6.3b-settings-ui
```

- [ ] **Step 3 : Ouvrir la PR avec checklist de smoke**

```bash
gh pr create --base main --head feat/phase-6.3b-settings-ui \
  --title "feat(phase-6.3b): onglet Settings menu-bar (config + CA)" \
  --body "$(cat <<'EOF'
Phase 6.3b — UI Settings consommant le backend 6.3a.

## Contenu
- 6e onglet Settings (Form/GroupBox).
- Affichage config.get ; édition hot (politique exfil, max subs/min, nb backups) via config.set, apply immédiat.
- CA : statut ca.is_trusted + Install/Uninstall (CATrustStore, seam testable).
- Reveal config.json (RPC config.path) + Reload.

## Smoke testing
- [ ] L'onglet Settings s'affiche et montre les vraies valeurs du daemon.
- [ ] Changer la politique exfil → appliqué (vérifié par `iris config get`) + backup créé.
- [ ] Modifier max subs/min → reflété ; valeur invalide → bandeau d'erreur, valeur revient à la vérité daemon.
- [ ] Modifier nb backups → reflété.
- [ ] Install CA → panneau d'auth → statut « Trusted » (corroboré `iris ca is-trusted`).
- [ ] Uninstall CA → « Not trusted ». Re-Install idempotent (la 2e fois, le bouton n'apparaît plus si déjà trusted).
- [ ] Reveal ouvre le Finder sur le config.json réel ; Reload re-lit après édition manuelle.
- [ ] Substitution `claude` inchangée après ajout de l'onglet.
EOF
)"
```

- [ ] **Step 4 : Vérifier le CI (oracle = API check-runs, PAS `gh pr checks`)**

```bash
SHA=$(git rev-parse HEAD)
gh api repos/:owner/:repo/commits/$SHA/check-runs --jq '.check_runs[] | "\(.name): \(.status)/\(.conclusion)"'
```
Expected: `build-test` et `xcode-build` (macos-15) en `completed/success`. Le gate xcodebuild est le seul juge de la vue.

- [ ] **Step 5 : Revue Gemini** (CLAUDE.md §8) — polling 1 min, fenêtre 10 min, plafond 30 min. Appliquer/refuser factuellement chaque commentaire, reply avec SHA.

- [ ] **Step 6 : Smoke poste** — cocher la checklist de la PR (au Mac). Couper un éventuel daemon dev avant, relancer un `irisd` frais. ⚠️ Re-signer `irisd` Developer ID APRÈS le dernier `swift build` si l'ACL Keychain (8b) doit fonctionner pour un vrai secret.

- [ ] **Step 7 : Merge sur confirmation explicite user** — `gh pr merge --squash` une fois les 3 conditions §8 réunies (Gemini traité, CI verte, checklist cochée).

---

## Self-review (rempli)

- **Couverture spec :** §3 contenu → Tasks 8/9 ; §4 cœur (AdminCalling, AppModel, seam) → Tasks 2-6 ; §5 RPC config.path → Task 1 ; §6 flux → Task 9 ; bootstrap → Task 7 ; §8 tests → Tasks 1-7 ; §9 DoD → Task 10. Aucun trou.
- **Placeholders :** aucun TODO/TBD ; tout code et toute commande sont concrets.
- **Cohérence des types :** `fetchConfig/setConfig/reloadConfig/configPath/isCATrusted/caExportPath` identiques entre protocole (T2), conformer (T2), mock (T2), AppModel (T3-5), bootstrap (T7). `CATrustInstalling.install/uninstall(pemPath:)` identiques entre protocole, `SystemCATrustInstaller`, `FakeCATrustInstaller`, et les appels `AppModel` (T6). `ConfigSetParams.Update(key:value:)`, `ConfigPathResult(path:)`, `ConfigSetResult(applied:requiresRestart:)`, `ConfigReloadResult(reloaded:ignored:)` conformes à `AdminProtocol.swift`.
- **Ordonnancement :** Task 9 (vue) référence l'API AppModel des Tasks 3-6 → faire 1→7 puis 8/9 ; Task 8 step 3 note que la vue est hors `swift build` (jugée CI).
```
