# Phase 6.3a — Modèle de configuration unifié (app-first) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer le couple `config.toml` (lecture seule, possédé par l'humain) + `runtime-rules.json` par un **fichier unique `config.json`** possédé par le daemon, seedé au 1er boot, mutable via RPC (`config.set` + `rule.*`), avec backups horodatés.

**Architecture:** Un acteur `ConfigStore` (IrisKit) devient la source de vérité : il charge/seed le fichier, valide avant chaque écriture, persiste en atomique 0600 et backupe avec rotation. Il absorbe `ConfigLoader` (parse TOML, supprimé) et `RuntimeRulesStore` (persistance hosts, supprimé). Le `Daemon` et l'`AdminDispatcher` sont recâblés autour de lui. L'API RPC au fil reste stable (un seul verbe ajouté : `config.set`).

**Tech Stack:** Swift 5.9+, SwiftPM, Foundation `JSONEncoder/Decoder`, Swift actors (`-strict-concurrency=complete`), XCTest. La dépendance `TOMLKit` est retirée.

**Référence spec:** `docs/superpowers/specs/2026-06-05-phase-6.3a-config-unifiee-design.md`

---

## Décisions verrouillées (rappel pour l'exécutant)

- **Greenfield** : aucune migration depuis un `config.toml` legacy (aucun n'est déployé).
- **Fichier corrompu au boot → échec bruyant** (refus de démarrer, pas de fallback silencieux, pas d'écrasement).
- **`MITMRule` (type au fil) reste inchangé** : il conserve son champ `source`. Le modèle interne `ConfigStore` ne stocke pas de `source` ; le dispatcher synthétise `source: .runtime` pour tous les hosts. **Raison :** l'UI Rules de la 6.2 (déjà mergée) affiche un cadenas pour `source == .toml` ; comme plus aucun host n'est `.toml`, le cadenas ne s'affiche plus et tous les hosts deviennent supprimables — comportement voulu, **sans toucher à l'app**.
- **`log_level` reste restart-required** (backend logging non câblé, hors-scope).
- **Hosts gérés par `rule.*`**, pas par `config.set` (ils ont validation + `created_at` propres).

## Structure des fichiers

**Créés :**
- `Sources/IrisKit/Config/ConfigStore.swift` — l'acteur source de vérité.
- `Tests/IrisKitTests/Config/ConfigStoreTests.swift` — tests du store.
- `Tests/IrisKitTests/Config/ConfigCodableTests.swift` — round-trip JSON du modèle.

**Modifiés :**
- `Sources/IrisKit/Config/Config.swift` — modèle étendu (`version`, `backups`, `hosts`).
- `Sources/IrisKit/IPC/AdminProtocol.swift` — verbe `config.set` + types.
- `Sources/IrisKit/IPC/AdminDispatcher.swift` — recâblage autour de `ConfigStore`.
- `Sources/irisd/Daemon.swift` — init + `reload()` autour de `ConfigStore`.
- `Sources/irisd/App.swift` — résolution `config.json`, création du `ConfigStore`.
- `Sources/iris/Commands/ConfigCommands.swift` — sous-commande `iris config set`.
- `Package.swift` — retrait de `TOMLKit`.
- `SPECS.md`, `docs/user-guide.md` — synchronisation TOML→JSON.

**Supprimés :**
- `Sources/IrisKit/Config/ConfigLoader.swift`
- `Sources/IrisKit/Rules/RuntimeRulesStore.swift`
- `Tests/IrisKitTests/ConfigTests.swift` (tests TOML, remplacés par ConfigCodableTests)
- `Tests/IrisKitTests/Rules/RuntimeRulesStoreTests.swift` (remplacé par ConfigStoreTests)

---

## Task 1 : Bascule du modèle de config vers `ConfigStore` (fondation atomique)

> **Note de build :** cette tâche change le type partagé `Config` et recâble tous ses
> consommateurs. Le build **ne recompile pas** entre les steps 1 et 7 ; il redevient
> vert au step 8. C'est volontaire — un changement de type central ne peut pas garder
> le vert au milieu. **Un seul commit** à la fin (step 9).

**Files:**
- Modify: `Sources/IrisKit/Config/Config.swift`
- Create: `Sources/IrisKit/Config/ConfigStore.swift`
- Create: `Tests/IrisKitTests/Config/ConfigCodableTests.swift`
- Create: `Tests/IrisKitTests/Config/ConfigStoreTests.swift`
- Modify: `Sources/irisd/App.swift:57, 77, 81-90, 112-124`
- Modify: `Sources/irisd/Daemon.swift:46-202, 254-344`
- Modify: `Sources/IrisKit/IPC/AdminDispatcher.swift:14-50, 178-228`
- Delete: `Sources/IrisKit/Config/ConfigLoader.swift`, `Sources/IrisKit/Rules/RuntimeRulesStore.swift`
- Delete: `Tests/IrisKitTests/ConfigTests.swift`, `Tests/IrisKitTests/Rules/RuntimeRulesStoreTests.swift`

- [ ] **Step 1 : Étendre le modèle `Config`**

Dans `Sources/IrisKit/Config/Config.swift`, remplacer le `struct Config` et l'ajout des types. Nouveau contenu de la déclaration `Config` + `BackupsConfig` + `HostEntry` (le reste du fichier — `BrokerConfig`, `LogLevel`, `SecurityConfig`, `ExfilAttemptPolicy`, `ConfigError` — reste **inchangé**, sauf suppression de `MITMHostEntry` et du cas `.tomlParseFailed` devenu inutile, qu'on **garde** par prudence wire) :

```swift
public struct Config: Codable, Sendable, Hashable {
    public let version: Int
    public let broker: BrokerConfig
    public let security: SecurityConfig
    public let backups: BackupsConfig
    public let hosts: [HostEntry]

    enum CodingKeys: String, CodingKey {
        case version
        case broker
        case security
        case backups
        case hosts
    }

    public init(
        version: Int = 1,
        broker: BrokerConfig,
        security: SecurityConfig,
        backups: BackupsConfig,
        hosts: [HostEntry]
    ) {
        self.version = version
        self.broker = broker
        self.security = security
        self.backups = backups
        self.hosts = hosts
    }

    /// Built-in defaults, used to seed `config.json` on first run.
    /// `hosts` carries an epoch sentinel `created_at`; `ConfigStore.seed()`
    /// rewrites it with the real seed timestamp (a static let can't call Date()).
    public static let `default` = Config(
        version: 1,
        broker: BrokerConfig(
            listen: "127.0.0.1:8888",
            eventsListen: "127.0.0.1:8899",
            adminSocket: "~/Library/Application Support/iris/admin.sock",
            logLevel: .info,
            eventRetentionDays: 7,
            eventRingSize: 10_000
        ),
        security: SecurityConfig(
            onExfilAttempt: .blockAndNotify,
            maxSubstitutionsPerMinute: 60
        ),
        backups: BackupsConfig(maxCount: 10),
        hosts: [HostEntry(host: "api.anthropic.com", createdAt: Date(timeIntervalSince1970: 0))]
    )
}

public struct BackupsConfig: Codable, Sendable, Hashable {
    public let maxCount: Int

    enum CodingKeys: String, CodingKey {
        case maxCount = "max_count"
    }

    public init(maxCount: Int) {
        self.maxCount = maxCount
    }
}

public struct HostEntry: Codable, Sendable, Hashable {
    public let host: String
    public let createdAt: Date

    enum CodingKeys: String, CodingKey {
        case host
        case createdAt = "created_at"
    }

    public init(host: String, createdAt: Date) {
        self.host = host
        self.createdAt = createdAt
    }
}
```

Étendre `validate()` (remplacer l'extension `Config.validate` et la `MITMHostEntry.validate`) :

```swift
extension Config {
    public func validate() throws {
        try broker.validate()
        try security.validate()
        try backups.validate()
        for entry in hosts {
            try entry.validate()
        }
    }
}

extension BackupsConfig {
    public func validate() throws {
        guard maxCount >= 1 else {
            throw ConfigError.invalidValue(field: "backups.max_count", value: "\(maxCount)")
        }
    }
}

extension HostEntry {
    public func validate() throws {
        guard Secret.isValidHost(host) else {
            throw ConfigError.invalidValue(field: "hosts.host", value: host)
        }
    }
}
```

Supprimer l'ancien `struct MITMHostEntry` et l'ancienne extension `MITMHostEntry.validate`.

- [ ] **Step 2 : Créer `ConfigStore`**

Créer `Sources/IrisKit/Config/ConfigStore.swift`. Le pattern de persistance atomique 0600 est repris de l'ex-`RuntimeRulesStore.persist()`.

```swift
import Foundation
import Logging

/// Single source of truth for IRIS configuration. Owns `config.json`; the daemon
/// is the only writer. Seeds the file on first run, validates before every write,
/// persists atomically (0600) and keeps timestamped backups with rotation.
///
/// Replaces `ConfigLoader` (TOML parse) and `RuntimeRulesStore` (host persistence).
public actor ConfigStore {
    public enum Error: Swift.Error, Equatable {
        case corrupted(String)
        case ioError(String)
        case invalidHost(String)
        case unknownKey(String)
        case invalidValue(field: String, value: String)
    }

    private let path: URL
    private let backupsDir: URL
    private let logger: Logger
    private var config: Config

    /// Loads `config.json` from `path`, or seeds it from `Config.default` if absent.
    /// A present-but-corrupted file throws `.corrupted` (fail loud — never overwrite).
    public init(path: URL, logger: Logger) async throws {
        self.path = path
        self.backupsDir = path.deletingLastPathComponent().appendingPathComponent("backups")
        self.logger = logger
        self.config = try Self.loadOrSeed(path: path, logger: logger)
    }

    // MARK: - Read

    public var current: Config { config }

    public func listHosts() -> [MITMRule] {
        config.hosts
            .sorted { $0.host < $1.host }
            .map { MITMRule(host: $0.host, createdAt: $0.createdAt, source: .runtime) }
    }

    public func allowedHosts() -> Set<String> {
        Set(config.hosts.map(\.host))
    }

    // MARK: - Host mutations (back `rule.*`)

    @discardableResult
    public func addHost(_ host: String, now: Date) throws -> MITMRule {
        guard Secret.isValidHost(host) else { throw Error.invalidHost(host) }
        if let existing = config.hosts.first(where: { $0.host == host }) {
            return MITMRule(host: existing.host, createdAt: existing.createdAt, source: .runtime)
        }
        let entry = HostEntry(host: host, createdAt: now)
        try persist(config.with(hosts: config.hosts + [entry]))
        return MITMRule(host: entry.host, createdAt: entry.createdAt, source: .runtime)
    }

    public func deleteHost(_ host: String) throws -> Bool {
        guard config.hosts.contains(where: { $0.host == host }) else { return false }
        try persist(config.with(hosts: config.hosts.filter { $0.host != host }))
        return true
    }

    // MARK: - Reload (manual file edit)

    /// Re-read the file from disk and adopt it. Returns the new config.
    public func reloadFromDisk() throws -> Config {
        let reloaded = try Self.load(path: path)
        try reloaded.validate()
        config = reloaded
        return reloaded
    }

    // MARK: - Loading

    private static func loadOrSeed(path: URL, logger: Logger) throws -> Config {
        if FileManager.default.fileExists(atPath: path.path) {
            return try load(path: path)
        }
        logger.info("config.json absent — seeding from defaults", metadata: ["path": "\(path.path)"])
        let seed = Config.default.with(
            hosts: Config.default.hosts.map { HostEntry(host: $0.host, createdAt: Date()) }
        )
        try seed.validate()
        try writeAtomic(seed, to: path)
        return seed
    }

    private static func load(path: URL) throws -> Config {
        let data: Data
        do {
            data = try Data(contentsOf: path)
        } catch {
            throw Error.ioError("read failed: \(error)")
        }
        do {
            return try makeDecoder().decode(Config.self, from: data)
        } catch {
            // Fail loud: a corrupted config must NOT be silently replaced.
            throw Error.corrupted("config.json parse failed: \(error)")
        }
    }

    // MARK: - Persistence + backups

    /// Validate → backup current on-disk file → write new atomically → rotate.
    private func persist(_ newConfig: Config) throws {
        try newConfig.validate()
        try backupCurrentFile(maxCount: newConfig.backups.maxCount)
        try Self.writeAtomic(newConfig, to: path)
        config = newConfig
    }

    /// Copy the current on-disk file into `backups/config-<ISO8601>.json`, then
    /// prune oldest beyond `maxCount`. Backup failure aborts the save.
    private func backupCurrentFile(maxCount: Int) throws {
        guard FileManager.default.fileExists(atPath: path.path) else { return }
        do {
            try FileManager.default.createDirectory(at: backupsDir, withIntermediateDirectories: true)
            let stamp = Self.timestampFormatter.string(from: Date())
            let dest = backupsDir.appendingPathComponent("config-\(stamp).json")
            let data = try Data(contentsOf: path)
            try data.write(to: dest, options: [.atomic])
            try FileManager.default.setAttributes([.posixPermissions: NSNumber(value: 0o600)], ofItemAtPath: dest.path)
            try rotateBackups(maxCount: maxCount)
        } catch {
            throw Error.ioError("backup failed: \(error)")
        }
    }

    private func rotateBackups(maxCount: Int) throws {
        let files = (try? FileManager.default.contentsOfDirectory(atPath: backupsDir.path)) ?? []
        let backups = files
            .filter { $0.hasPrefix("config-") && $0.hasSuffix(".json") }
            .sorted()  // ISO8601 in the name sorts chronologically
        guard backups.count > maxCount else { return }
        for name in backups.prefix(backups.count - maxCount) {
            try? FileManager.default.removeItem(at: backupsDir.appendingPathComponent(name))
        }
    }

    private static func writeAtomic(_ config: Config, to path: URL) throws {
        let data: Data
        do {
            data = try makeEncoder().encode(config)
        } catch {
            throw Error.ioError("encode failed: \(error)")
        }
        let parent = path.deletingLastPathComponent()
        try? FileManager.default.createDirectory(at: parent, withIntermediateDirectories: true)
        let tmp = parent.appendingPathComponent(".\(path.lastPathComponent).tmp-\(UUID().uuidString)")
        do {
            try data.write(to: tmp, options: [.atomic])
            try FileManager.default.setAttributes([.posixPermissions: NSNumber(value: 0o600)], ofItemAtPath: tmp.path)
            _ = try FileManager.default.replaceItemAt(path, withItemAt: tmp)
        } catch {
            try? FileManager.default.removeItem(at: tmp)
            throw Error.ioError("write failed: \(error)")
        }
    }

    private static func makeEncoder() -> JSONEncoder {
        let e = JSONEncoder()
        e.dateEncodingStrategy = .iso8601
        e.outputFormatting = [.prettyPrinted, .sortedKeys]
        return e
    }

    private static func makeDecoder() -> JSONDecoder {
        let d = JSONDecoder()
        d.dateDecodingStrategy = .iso8601
        return d
    }

    private static let timestampFormatter: ISO8601DateFormatter = {
        let f = ISO8601DateFormatter()
        // Colons are illegal-ish in filenames on some FS; use a safe variant.
        f.formatOptions = [.withYear, .withMonth, .withDay, .withTime, .withTimeZone]
        return f
    }()
}

extension Config {
    /// Returns a copy with `hosts` replaced.
    func with(hosts: [HostEntry]) -> Config {
        Config(version: version, broker: broker, security: security, backups: backups, hosts: hosts)
    }
}
```

> **Note timestamp fichiers :** `ISO8601DateFormatter` produit `2026-06-05T12:00:00Z`
> avec des `:` — acceptés par APFS mais on évite l'ambiguïté en triant sur la chaîne
> (l'ordre lexicographique ISO8601 == ordre chronologique). Si un `:` pose problème
> en exécution, remplacer par un format `yyyyMMdd-HHmmss` via `DateFormatter` (locale
> `en_US_POSIX`, TZ UTC) — garder le tri lexicographique == chronologique.

- [ ] **Step 3 : Écrire `ConfigCodableTests`**

Créer `Tests/IrisKitTests/Config/ConfigCodableTests.swift` :

```swift
import Foundation
import XCTest

@testable import IrisKit

final class ConfigCodableTests: XCTestCase {
    func testDefaultRoundTripsThroughJSON() throws {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601

        let data = try encoder.encode(Config.default)
        let decoded = try decoder.decode(Config.self, from: data)

        XCTAssertEqual(decoded.version, 1)
        XCTAssertEqual(decoded.broker.listen, "127.0.0.1:8888")
        XCTAssertEqual(decoded.security.onExfilAttempt, .blockAndNotify)
        XCTAssertEqual(decoded.backups.maxCount, 10)
        XCTAssertEqual(decoded.hosts.map(\.host), ["api.anthropic.com"])
    }

    func testSnakeCaseKeysOnWire() throws {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        let json = String(data: try encoder.encode(Config.default), encoding: .utf8)!
        XCTAssertTrue(json.contains("\"on_exfil_attempt\""))
        XCTAssertTrue(json.contains("\"max_substitutions_per_minute\""))
        XCTAssertTrue(json.contains("\"event_retention_days\""))
        XCTAssertTrue(json.contains("\"max_count\""))
        XCTAssertTrue(json.contains("\"created_at\""))
    }

    func testValidateRejectsZeroBackupsMaxCount() {
        let c = Config.default.with(hosts: Config.default.hosts)
        let bad = Config(
            version: 1, broker: c.broker, security: c.security,
            backups: BackupsConfig(maxCount: 0), hosts: c.hosts
        )
        XCTAssertThrowsError(try bad.validate()) { error in
            guard case ConfigError.invalidValue(let field, _) = error else {
                return XCTFail("expected invalidValue, got \(error)")
            }
            XCTAssertEqual(field, "backups.max_count")
        }
    }

    func testValidateRejectsInvalidHost() {
        let bad = Config(
            version: 1, broker: Config.default.broker, security: Config.default.security,
            backups: Config.default.backups,
            hosts: [HostEntry(host: "_not_a_host_", createdAt: Date())]
        )
        XCTAssertThrowsError(try bad.validate()) { error in
            guard case ConfigError.invalidValue(let field, _) = error else {
                return XCTFail("expected invalidValue, got \(error)")
            }
            XCTAssertEqual(field, "hosts.host")
        }
    }
}
```

- [ ] **Step 4 : Écrire `ConfigStoreTests`**

Créer `Tests/IrisKitTests/Config/ConfigStoreTests.swift` :

```swift
import Foundation
import Logging
import XCTest

@testable import IrisKit

final class ConfigStoreTests: XCTestCase {
    var tmpDir: URL!
    var path: URL!
    let logger = Logger(label: "t")

    override func setUp() async throws {
        tmpDir = FileManager.default.temporaryDirectory
            .appendingPathComponent("iris-configstore-\(UUID().uuidString)")
        try FileManager.default.createDirectory(at: tmpDir, withIntermediateDirectories: true)
        path = tmpDir.appendingPathComponent("config.json")
    }

    override func tearDown() async throws {
        try? FileManager.default.removeItem(at: tmpDir)
    }

    func testSeedsWhenFileAbsent() async throws {
        XCTAssertFalse(FileManager.default.fileExists(atPath: path.path))
        let store = try await ConfigStore(path: path, logger: logger)
        XCTAssertTrue(FileManager.default.fileExists(atPath: path.path))
        let cfg = await store.current
        XCTAssertEqual(cfg.version, 1)
        XCTAssertEqual(cfg.hosts.map(\.host), ["api.anthropic.com"])
        // Seed timestamp is "now", not the epoch sentinel.
        XCTAssertGreaterThan(cfg.hosts[0].createdAt.timeIntervalSince1970, 1_000_000_000)
    }

    func testSeededFilePermissionsAre0600() async throws {
        _ = try await ConfigStore(path: path, logger: logger)
        let attrs = try FileManager.default.attributesOfItem(atPath: path.path)
        let perm = (attrs[.posixPermissions] as? NSNumber)?.intValue ?? 0
        XCTAssertEqual(perm, 0o600, "got \(String(perm, radix: 8))")
    }

    func testAddHostPersistsAndReloadsInNewInstance() async throws {
        let s1 = try await ConfigStore(path: path, logger: logger)
        _ = try await s1.addHost("api.example.com", now: Date())
        let s2 = try await ConfigStore(path: path, logger: logger)
        let hosts = await s2.listHosts().map(\.host)
        XCTAssertEqual(hosts, ["api.anthropic.com", "api.example.com"])
    }

    func testAddHostIsIdempotent() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        _ = try await store.addHost("api.example.com", now: Date())
        _ = try await store.addHost("api.example.com", now: Date().addingTimeInterval(60))
        let count = await store.listHosts().filter { $0.host == "api.example.com" }.count
        XCTAssertEqual(count, 1)
    }

    func testAddHostRejectsInvalid() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        await assertThrowsAsync(try await store.addHost("has space.com", now: Date()))
        await assertThrowsAsync(try await store.addHost("API.example.com", now: Date()))
    }

    func testDeleteHost() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        _ = try await store.addHost("api.example.com", now: Date())
        let removed = try await store.deleteHost("api.example.com")
        XCTAssertTrue(removed)
        let absent = try await store.deleteHost("nope.example.com")
        XCTAssertFalse(absent)
    }

    func testBackupCreatedAndRotatedToMaxCount() async throws {
        // Seed maxCount=3 by writing a config first, then drive >3 saves.
        let store = try await ConfigStore(path: path, logger: logger)
        // Each addHost triggers a save (and a backup of the prior file).
        for i in 0..<6 {
            _ = try await store.addHost("h\(i).example.com", now: Date().addingTimeInterval(Double(i)))
        }
        let backupsDir = tmpDir.appendingPathComponent("backups")
        let files = (try? FileManager.default.contentsOfDirectory(atPath: backupsDir.path)) ?? []
        let backups = files.filter { $0.hasPrefix("config-") && $0.hasSuffix(".json") }
        // Default maxCount is 10 → all 6 prior states kept.
        XCTAssertEqual(backups.count, 6)
    }

    func testBackupRotationHonoursLoweredMaxCount() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        // Lower the cap to 2 via a host add carrying a config whose backups.max_count=2.
        // (config.set lands in Task 2; here we drive the store's own write path.)
        // Drive saves with maxCount=2 by reloading a hand-written config.
        let lowered = Config.default.with(hosts: Config.default.hosts)
        let lowered2 = Config(
            version: 1, broker: lowered.broker, security: lowered.security,
            backups: BackupsConfig(maxCount: 2), hosts: lowered.hosts
        )
        let enc = JSONEncoder()
        enc.dateEncodingStrategy = .iso8601
        try enc.encode(lowered2).write(to: path)
        _ = try await store.reloadFromDisk()
        for i in 0..<5 {
            _ = try await store.addHost("h\(i).example.com", now: Date().addingTimeInterval(Double(i)))
        }
        let backupsDir = tmpDir.appendingPathComponent("backups")
        let files = (try? FileManager.default.contentsOfDirectory(atPath: backupsDir.path)) ?? []
        let backups = files.filter { $0.hasPrefix("config-") && $0.hasSuffix(".json") }
        XCTAssertLessThanOrEqual(backups.count, 2)
    }

    func testCorruptedFileThrowsAtBoot() async throws {
        try Data("not valid json {{{".utf8).write(to: path)
        do {
            _ = try await ConfigStore(path: path, logger: logger)
            XCTFail("expected ConfigStore to throw on corrupted file")
        } catch let error as ConfigStore.Error {
            guard case .corrupted = error else {
                return XCTFail("expected .corrupted, got \(error)")
            }
        }
        // The corrupted file must NOT have been overwritten.
        let onDisk = try String(contentsOf: path, encoding: .utf8)
        XCTAssertTrue(onDisk.contains("not valid json"))
    }
}
```

(`assertThrowsAsync` est défini dans `RuntimeRulesStoreTests.swift` qu'on supprime — le déplacer : créer `Tests/IrisKitTests/TestSupport.swift` contenant l'helper `assertThrowsAsync` copié verbatim depuis l'ancien fichier, lignes 128-140.)

- [ ] **Step 5 : Recâbler `App.swift`**

Dans `Sources/irisd/App.swift` :

- Remplacer la fonction `loadConfig()` (lignes 112-124) et le bloc `resolvedConfigPath` (lignes 81-90) par une création de `ConfigStore`. La nouvelle forme de `run()` :

Remplacer les lignes 57 (`let config = try loadConfig()`) … 99 (fin de l'init `Daemon`) par :

```swift
        let resolvedConfigPath: URL =
            configPath.map { URL(fileURLWithPath: ($0 as NSString).expandingTildeInPath) }
            ?? URL(fileURLWithPath: ("~/Library/Application Support/iris/config.json" as NSString).expandingTildeInPath)

        var bootLogger = Logger(label: "io.iris.daemon")
        bootLogger.logLevel = .info
        let configStore = try await ConfigStore(path: resolvedConfigPath, logger: bootLogger)
        let config = await configStore.current

        var logger = Logger(label: "io.iris.daemon")
        let level = logLevel.flatMap(Self.parseLogLevel(_:)) ?? Self.loggerLevel(from: config.broker.logLevel)
        logger.logLevel = level

        let caURL: URL
        if let caPath = caPath {
            caURL = URL(fileURLWithPath: (caPath as NSString).expandingTildeInPath)
        } else {
            let support = ("~/Library/Application Support/iris/ca.pem" as NSString).expandingTildeInPath
            caURL = URL(fileURLWithPath: support)
        }

        logger.info(
            "Starting irisd",
            metadata: [
                "listen": "\(config.broker.listen)",
                "events_listen": "\(config.broker.eventsListen)",
                "admin_socket": "\(config.broker.adminSocket)",
                "allowed_hosts": "\(config.hosts.map(\.host).sorted())",
            ]
        )

        let daemon = try await Daemon(
            configStore: configStore,
            secretBackend: inMemorySecrets ? .inMemoryFromEnvironment : .keychain,
            caBackend: inMemoryCa ? .inMemory : .keychain,
            caPath: caURL,
            logger: logger
        )
```

- Supprimer la fonction `loadConfig()` (lignes 112-124).
- Mettre à jour l'aide du flag (`App.swift:20`) : `"Config JSON path (default ~/Library/Application Support/iris/config.json; seeded with defaults if absent)."`

- [ ] **Step 6 : Recâbler `Daemon.swift`**

Dans `Sources/irisd/Daemon.swift` :

- Remplacer la propriété `private let runtimeRulesStore: RuntimeRulesStore` (ligne 38) par `private let configStore: ConfigStore`.
- Supprimer `reloadBox` (ligne 39) et `tomlHostsBox` (ligne 43) — le `ConfigStore` est désormais la source de vérité ; le reload appelle `self.reload()` directement via une closure simple.
- Changer la signature `init` (ligne 46-53) : remplacer `config: Config, configPath: URL? = nil` par `configStore: ConfigStore`. En tête d'init :

```swift
    public init(
        configStore: ConfigStore,
        secretBackend: SecretBackend,
        caBackend: CABackend = .keychain,
        caPath: URL,
        logger: Logger
    ) async throws {
        let config = await configStore.current
        try config.validate()
        self.currentConfig = config
        self.configStore = configStore
        self.logger = logger
        self.eventLoopGroup = MultiThreadedEventLoopGroup(numberOfThreads: System.coreCount)
        ...
```

- Remplacer le bloc runtime-rules (lignes 91-108) par :

```swift
        let allowedHosts = await configStore.allowedHosts()
```

(supprimer la création de `runtimeRulesPath`, `RuntimeRulesStore`, `tomlHosts`/`runtimeHosts`/`tomlHostsBox`.)

- Dans la construction du `AdminDispatcher` (lignes 152-172), remplacer les paramètres `config:`, `runtimeRulesStore:`, `onRulesChanged:`, `tomlHostsProvider:`, `onConfigReload:` par le nouveau jeu (voir Step 7) :

```swift
        let capturedStore = configStore
        let dispatcher = AdminDispatcher(
            secretStore: secretStore,
            eventRing: proxy.eventRing,
            caManager: caManager,
            daemon: daemonControl,
            configStore: configStore,
            onHostsChanged: { [capturedProxy, capturedStore] in
                await capturedProxy.updateAllowedHosts(await capturedStore.allowedHosts())
            },
            onSecurityChanged: { [capturedProxy, capturedStore] in
                let cfg = await capturedStore.current
                await capturedProxy.updateSecurityPolicy(
                    maxSubstitutionsPerMinute: cfg.security.maxSubstitutionsPerMinute,
                    onExfilAttempt: cfg.security.onExfilAttempt
                )
            },
            logger: logger
        )
```

- Supprimer le bloc `reloadBox.withLockedValue` (lignes 199-201).
- Réécrire `reload()` (lignes 254-344). Nouvelle version : relit le fichier via `configStore`, calcule `ignored` (champs structurels modifiés), applique les champs chauds :

```swift
    public func reload() async throws -> ConfigReloadResult {
        let newConfig: Config
        do {
            newConfig = try await configStore.reloadFromDisk()
        } catch {
            logger.warning("config.reload failed", metadata: ["error": "\(error)"])
            throw JSONRPCError.configReloadFailed("\(error)")
        }

        var ignored: [String] = []
        let old = currentConfig
        if newConfig.broker.listen != old.broker.listen { ignored.append("broker.listen") }
        if newConfig.broker.eventsListen != old.broker.eventsListen { ignored.append("broker.events_listen") }
        if newConfig.broker.adminSocket != old.broker.adminSocket { ignored.append("broker.admin_socket") }
        if newConfig.broker.eventRetentionDays != old.broker.eventRetentionDays { ignored.append("broker.event_retention_days") }
        if newConfig.broker.eventRingSize != old.broker.eventRingSize { ignored.append("broker.event_ring_size") }
        if newConfig.broker.logLevel != old.broker.logLevel { ignored.append("broker.log_level") }

        await proxy.updateSecurityPolicy(
            maxSubstitutionsPerMinute: newConfig.security.maxSubstitutionsPerMinute,
            onExfilAttempt: newConfig.security.onExfilAttempt
        )
        currentConfig = newConfig
        await proxy.updateAllowedHosts(Set(newConfig.hosts.map(\.host)))

        logger.info("config.reload OK", metadata: ["ignored": "\(ignored)"])
        return ConfigReloadResult(reloaded: true, ignored: ignored)
    }
```

(Les helpers `host(of:)`/`port(of:)` lignes 348-362 restent inchangés.)

- [ ] **Step 7 : Recâbler `AdminDispatcher.swift` + supprimer l'ancien**

Dans `Sources/IrisKit/IPC/AdminDispatcher.swift` :

- Remplacer les propriétés/init (lignes 14-50). Retirer `config`, `runtimeRulesStore`, `onRulesChanged`, `tomlHostsProvider`, `onConfigReload`. Ajouter `configStore`, `onHostsChanged`, `onSecurityChanged` :

```swift
public struct AdminDispatcher: Sendable {
    public let secretStore: any SecretStore
    public let eventRing: EventRing
    public let caManager: CAManager
    public let daemon: any DaemonControl
    public let configStore: ConfigStore
    public let onHostsChanged: @Sendable () async -> Void
    public let onSecurityChanged: @Sendable () async -> Void
    public let logger: Logger

    public init(
        secretStore: any SecretStore,
        eventRing: EventRing,
        caManager: CAManager,
        daemon: any DaemonControl,
        configStore: ConfigStore,
        onHostsChanged: @escaping @Sendable () async -> Void = {},
        onSecurityChanged: @escaping @Sendable () async -> Void = {},
        logger: Logger = Logger(label: "io.iris.admin.dispatcher")
    ) {
        self.secretStore = secretStore
        self.eventRing = eventRing
        self.caManager = caManager
        self.daemon = daemon
        self.configStore = configStore
        self.onHostsChanged = onHostsChanged
        self.onSecurityChanged = onSecurityChanged
        self.logger = logger
    }
```

- Le `catch let error as RuntimeRulesStore.Error` (lignes 69-81) devient `catch let error as ConfigStore.Error` :

```swift
        } catch let error as ConfigStore.Error {
            switch error {
            case .invalidHost(let h):
                return .failure(id: request.id, error: JSONRPCError(code: JSONRPCError.invalidParams.code, message: "invalid host: \(h)"))
            case .unknownKey(let k):
                return .failure(id: request.id, error: JSONRPCError(code: JSONRPCError.invalidParams.code, message: "unknown key: \(k)"))
            case .invalidValue(let field, let value):
                return .failure(id: request.id, error: JSONRPCError(code: JSONRPCError.invalidParams.code, message: "invalid value '\(value)' for '\(field)'"))
            case .corrupted(let msg), .ioError(let msg):
                return .failure(id: request.id, error: JSONRPCError(code: JSONRPCError.internalError.code, message: msg))
            }
        }
```

- Remplacer `case .configGet` (lignes 178-182), `case .ruleAdd` (184-200), `case .ruleList` (202-213), `case .ruleDelete` (215-224), `case .configReload` (226-228) par :

```swift
        case .configGet:
            return try JSONValue.encoding(await configStore.current)

        case .ruleAdd:
            let payload = try Self.decode(RuleHostParams.self, from: params)
            let rule = try await configStore.addHost(payload.host, now: Date())
            await onHostsChanged()
            return try JSONValue.encoding(rule)

        case .ruleList:
            return try JSONValue.encoding(await configStore.listHosts())

        case .ruleDelete:
            let payload = try Self.decode(RuleHostParams.self, from: params)
            let deleted = try await configStore.deleteHost(payload.host)
            guard deleted else { throw JSONRPCError.ruleNotFound }
            await onHostsChanged()
            return try JSONValue.encoding(RuleDeletedResult(deleted: true))

        case .configReload:
            // reload is owned by the daemon (applies hot fields to the proxy);
            // wired via onSecurityChanged + onHostsChanged is insufficient here,
            // so configReload is handled by the daemon's reload path through the
            // onConfigReload closure on Daemon. Keep delegating:
            let cfg = try await configStore.reloadFromDisk()
            await onSecurityChanged()
            await onHostsChanged()
            return try JSONValue.encoding(ConfigReloadResult(reloaded: true, ignored: ignoredStructural(cfg)))
```

> **Decision — `config.reload` ownership:** pour éviter de dupliquer le calcul de
> `ignored` entre `Daemon.reload()` et le dispatcher, le dispatcher délègue le
> reload au `configStore` puis applique les champs chauds via `onSecurityChanged`
> + `onHostsChanged`, et calcule `ignored` via un helper local `ignoredStructural`.
> Le SIGHUP (`App.swift:104-106`) continue d'appeler `daemon.reload()` directement.
> Les deux chemins lisent le même fichier via `configStore` — cohérents. Ajouter ce
> helper privé dans `AdminDispatcher` :

```swift
    private func ignoredStructural(_ new: Config) -> [String] {
        // The dispatcher has no "old" config snapshot; structural changes via a
        // manual file edit are surfaced by the daemon's SIGHUP reload log. For
        // the RPC path we report the structural keys conservatively as the set
        // that requires restart to take effect. Callers (UI) treat this as
        // "these may need a restart".
        ["broker.listen", "broker.events_listen", "broker.admin_socket",
         "broker.event_retention_days", "broker.event_ring_size", "broker.log_level"]
            .filter { _ in false }  // RPC reload reports none; SIGHUP path logs specifics
    }
```

> Note: le helper renvoie `[]` pour le chemin RPC (le calcul fin appartient à
> `Daemon.reload()` qui a l'ancien snapshot). C'est volontaire et documenté ; la
> 6.3b utilise plutôt `config.set` (qui retourne `requires_restart` précis) pour
> les changements pilotés par l'UI, pas `config.reload`.

- Supprimer les fichiers : `Sources/IrisKit/Config/ConfigLoader.swift`, `Sources/IrisKit/Rules/RuntimeRulesStore.swift`, `Tests/IrisKitTests/ConfigTests.swift`, `Tests/IrisKitTests/Rules/RuntimeRulesStoreTests.swift`.
- Adapter `Tests/IrisKitTests/ConfigReloadDispatcherTests.swift` : il construit un `AdminDispatcher` avec l'ancienne signature → le mettre à jour pour passer un `ConfigStore` temporaire (créé sur un `tmpDir`) au lieu de `config:`/`runtimeRulesStore:`. (Lire le fichier, adapter l'init et les assertions au nouveau wiring ; conserver l'intention : un reload renvoie un `ConfigReloadResult`.)

- [ ] **Step 8 : Build + tests verts**

Run: `swift build 2>&1 | tail -20`
Expected: build succeeds, no error.

Run: `swift test 2>&1 | tail -30`
Expected: all tests pass (incluant `ConfigStoreTests`, `ConfigCodableTests` ; `ConfigTests`/`RuntimeRulesStoreTests` supprimés).

Run: `swift-format lint --recursive Sources Tests 2>&1 | tail -5`
Expected: aucun diagnostic.

- [ ] **Step 9 : Commit**

```bash
git add -A
git commit -m "feat(phase-6.3a): ConfigStore unifié — config.json app-first

Remplace ConfigLoader (TOML) + RuntimeRulesStore par un acteur ConfigStore
unique : seed au 1er boot, validate avant write, persistance atomique 0600,
backups horodatés avec rotation. Recâble Daemon/AdminDispatcher/App. Modèle
Config étendu (version, backups, hosts). MITMRule (wire) inchangé."
```

---

## Task 2 : RPC `config.set` (mutation des réglages scalaires)

**Files:**
- Modify: `Sources/IrisKit/IPC/AdminProtocol.swift:6-28` (case), `:113-207` (types)
- Modify: `Sources/IrisKit/Config/ConfigStore.swift` (méthode `applyUpdates`)
- Modify: `Sources/IrisKit/IPC/AdminDispatcher.swift` (case `.configSet`)
- Create/Modify: `Tests/IrisKitTests/Config/ConfigStoreTests.swift` (tests `applyUpdates`)

- [ ] **Step 1 : Ajouter le verbe + types au protocole**

Dans `AdminProtocol.swift`, ajouter au `enum AdminMethod` (après `case configReload`, ligne 26) :

```swift
    case configSet = "config.set"
```

Ajouter les types (section Params/Results) :

```swift
public struct ConfigSetParams: Codable, Sendable, Equatable {
    public struct Update: Codable, Sendable, Equatable {
        public let key: String
        public let value: String
        public init(key: String, value: String) {
            self.key = key
            self.value = value
        }
    }
    public let updates: [Update]
    public init(updates: [Update]) { self.updates = updates }
}

public struct ConfigSetResult: Codable, Sendable, Equatable {
    public let applied: [String]
    public let requiresRestart: [String]
    enum CodingKeys: String, CodingKey {
        case applied
        case requiresRestart = "requires_restart"
    }
    public init(applied: [String], requiresRestart: [String]) {
        self.applied = applied
        self.requiresRestart = requiresRestart
    }
}
```

- [ ] **Step 2 : Écrire le test `applyUpdates` (échoue d'abord)**

Ajouter à `ConfigStoreTests.swift` :

```swift
    func testApplyUpdatesHotFieldPersistsAndReportsApplied() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        let result = try await store.applyUpdates([
            .init(key: "security.on_exfil_attempt", value: "block_only"),
            .init(key: "security.max_substitutions_per_minute", value: "30"),
        ])
        XCTAssertEqual(Set(result.applied), ["security.on_exfil_attempt", "security.max_substitutions_per_minute"])
        XCTAssertEqual(result.requiresRestart, [])
        let cfg = await store.current
        XCTAssertEqual(cfg.security.onExfilAttempt, .blockOnly)
        XCTAssertEqual(cfg.security.maxSubstitutionsPerMinute, 30)
    }

    func testApplyUpdatesStructuralFieldReportsRequiresRestart() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        let result = try await store.applyUpdates([
            .init(key: "broker.listen", value: "127.0.0.1:9999"),
        ])
        XCTAssertEqual(result.requiresRestart, ["broker.listen"])
        XCTAssertEqual(result.applied, [])
        let cfg = await store.current
        XCTAssertEqual(cfg.broker.listen, "127.0.0.1:9999")  // persisted regardless
    }

    func testApplyUpdatesUnknownKeyThrows() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        await assertThrowsAsync(try await store.applyUpdates([.init(key: "broker.nope", value: "x")]))
    }

    func testApplyUpdatesInvalidValueThrowsAndLeavesFileIntact() async throws {
        let store = try await ConfigStore(path: path, logger: logger)
        let before = await store.current.security.maxSubstitutionsPerMinute
        await assertThrowsAsync(try await store.applyUpdates([.init(key: "security.max_substitutions_per_minute", value: "0")]))
        let after = await store.current.security.maxSubstitutionsPerMinute
        XCTAssertEqual(before, after, "rejected update must not mutate state")
    }
```

Where the test type is `ConfigStore.applyUpdates(_:)` taking `[ConfigSetParams.Update]` and returning `ConfigSetResult`.

Run: `swift test --filter ConfigStoreTests 2>&1 | tail -20`
Expected: FAIL (`applyUpdates` not defined).

- [ ] **Step 3 : Implémenter `applyUpdates` dans `ConfigStore`**

Ajouter à `ConfigStore` :

```swift
    /// Hot fields take effect at runtime; structural fields persist but require a
    /// restart. Hosts are NOT settable here (use addHost/deleteHost).
    private static let structuralKeys: Set<String> = [
        "broker.listen", "broker.events_listen", "broker.admin_socket",
        "broker.event_retention_days", "broker.event_ring_size", "broker.log_level",
    ]
    private static let hotKeys: Set<String> = [
        "security.on_exfil_attempt", "security.max_substitutions_per_minute", "backups.max_count",
    ]

    public func applyUpdates(_ updates: [ConfigSetParams.Update]) throws -> ConfigSetResult {
        var broker = config.broker
        var security = config.security
        var backups = config.backups
        var applied: [String] = []
        var requiresRestart: [String] = []

        for u in updates {
            let known = Self.structuralKeys.contains(u.key) || Self.hotKeys.contains(u.key)
            guard known else { throw Error.unknownKey(u.key) }
            switch u.key {
            case "security.on_exfil_attempt":
                guard let p = ExfilAttemptPolicy(rawValue: u.value) else { throw Error.invalidValue(field: u.key, value: u.value) }
                security = SecurityConfig(onExfilAttempt: p, maxSubstitutionsPerMinute: security.maxSubstitutionsPerMinute)
            case "security.max_substitutions_per_minute":
                guard let n = Int(u.value) else { throw Error.invalidValue(field: u.key, value: u.value) }
                security = SecurityConfig(onExfilAttempt: security.onExfilAttempt, maxSubstitutionsPerMinute: n)
            case "backups.max_count":
                guard let n = Int(u.value) else { throw Error.invalidValue(field: u.key, value: u.value) }
                backups = BackupsConfig(maxCount: n)
            case "broker.log_level":
                guard let l = LogLevel(rawValue: u.value) else { throw Error.invalidValue(field: u.key, value: u.value) }
                broker = Self.broker(broker, settingLogLevel: l)
            case "broker.listen":
                broker = Self.broker(broker, settingListen: u.value)
            case "broker.events_listen":
                broker = Self.broker(broker, settingEventsListen: u.value)
            case "broker.admin_socket":
                broker = Self.broker(broker, settingAdminSocket: u.value)
            case "broker.event_retention_days":
                guard let n = Int(u.value) else { throw Error.invalidValue(field: u.key, value: u.value) }
                broker = Self.broker(broker, settingRetentionDays: n)
            case "broker.event_ring_size":
                guard let n = Int(u.value) else { throw Error.invalidValue(field: u.key, value: u.value) }
                broker = Self.broker(broker, settingRingSize: n)
            default:
                throw Error.unknownKey(u.key)
            }
            if Self.hotKeys.contains(u.key) { applied.append(u.key) } else { requiresRestart.append(u.key) }
        }

        let candidate = Config(version: config.version, broker: broker, security: security, backups: backups, hosts: config.hosts)
        do {
            try persist(candidate)  // validates, backs up, writes atomically
        } catch let e as Error {
            throw e
        }
        return ConfigSetResult(applied: applied, requiresRestart: requiresRestart)
    }

    // BrokerConfig has immutable lets; these helpers rebuild it field-by-field.
    private static func broker(_ b: BrokerConfig, settingLogLevel v: LogLevel) -> BrokerConfig { BrokerConfig(listen: b.listen, eventsListen: b.eventsListen, adminSocket: b.adminSocket, logLevel: v, eventRetentionDays: b.eventRetentionDays, eventRingSize: b.eventRingSize) }
    private static func broker(_ b: BrokerConfig, settingListen v: String) -> BrokerConfig { BrokerConfig(listen: v, eventsListen: b.eventsListen, adminSocket: b.adminSocket, logLevel: b.logLevel, eventRetentionDays: b.eventRetentionDays, eventRingSize: b.eventRingSize) }
    private static func broker(_ b: BrokerConfig, settingEventsListen v: String) -> BrokerConfig { BrokerConfig(listen: b.listen, eventsListen: v, adminSocket: b.adminSocket, logLevel: b.logLevel, eventRetentionDays: b.eventRetentionDays, eventRingSize: b.eventRingSize) }
    private static func broker(_ b: BrokerConfig, settingAdminSocket v: String) -> BrokerConfig { BrokerConfig(listen: b.listen, eventsListen: b.eventsListen, adminSocket: v, logLevel: b.logLevel, eventRetentionDays: b.eventRetentionDays, eventRingSize: b.eventRingSize) }
    private static func broker(_ b: BrokerConfig, settingRetentionDays v: Int) -> BrokerConfig { BrokerConfig(listen: b.listen, eventsListen: b.eventsListen, adminSocket: b.adminSocket, logLevel: b.logLevel, eventRetentionDays: v, eventRingSize: b.eventRingSize) }
    private static func broker(_ b: BrokerConfig, settingRingSize v: Int) -> BrokerConfig { BrokerConfig(listen: b.listen, eventsListen: b.eventsListen, adminSocket: b.adminSocket, logLevel: b.logLevel, eventRetentionDays: b.eventRetentionDays, eventRingSize: v) }
```

> **Note (Rule: invalid update must not mutate):** `persist` valide le `candidate`
> AVANT d'écrire ; si la validation échoue, `config` (l'état en mémoire) n'est jamais
> réassigné (réassignation seulement à la dernière ligne de `persist`). Le test
> `testApplyUpdatesInvalidValueThrowsAndLeavesFileIntact` le verrouille.

Run: `swift test --filter ConfigStoreTests 2>&1 | tail -20`
Expected: PASS.

- [ ] **Step 4 : Router `config.set` dans le dispatcher**

Dans `AdminDispatcher.handle`, ajouter un `case` :

```swift
        case .configSet:
            let payload = try Self.decode(ConfigSetParams.self, from: params)
            let result = try await configStore.applyUpdates(payload.updates)
            if !result.applied.isEmpty { await onSecurityChanged() }
            return try JSONValue.encoding(result)
```

> `onSecurityChanged` ré-applique `max_substitutions_per_minute` + `on_exfil_attempt`
> au proxy à chaud. `backups.max_count` n'a pas besoin d'application runtime (relu à
> chaque save). Les champs `requiresRestart` ne sont pas appliqués (par définition).

- [ ] **Step 5 : Build + tests + lint verts**

Run: `swift build 2>&1 | tail -5` → succeeds
Run: `swift test 2>&1 | tail -20` → all pass
Run: `swift-format lint --recursive Sources Tests 2>&1 | tail -5` → clean

- [ ] **Step 6 : Commit**

```bash
git add -A
git commit -m "feat(phase-6.3a): RPC config.set — mutation des réglages scalaires

Verbe config.set (clés en allow-list dot-path), applyUpdates côté ConfigStore
(valide → applique hot / persiste structurel → backup), retourne {applied,
requires_restart}. Hosts restent gérés par rule.*."
```

---

## Task 3 : CLI `iris config set`

**Files:**
- Modify: `Sources/iris/Commands/ConfigCommands.swift` (sous-commande `Set`)
- Create: `Tests/IntegrationTests/CLIConfigSetTests.swift`

- [ ] **Step 1 : Ajouter la sous-commande `Set`**

Dans `ConfigCommands.swift`, ajouter `Set.self` à `subcommands` (ligne 9) et la struct :

```swift
    struct Set: AsyncParsableCommand {
        static let configuration = CommandConfiguration(
            commandName: "set",
            abstract: "Set a configuration value (e.g. security.on_exfil_attempt block_only)."
        )

        @OptionGroup var connection: ConnectionOptions
        @Argument(help: "Config key (dot-path), e.g. security.max_substitutions_per_minute.")
        var key: String
        @Argument(help: "New value.")
        var value: String
        @Flag(name: .customLong("json")) var json: Bool = false

        mutating func run() async throws {
            let params = ConfigSetParams(updates: [.init(key: key, value: value)])
            let result = try await withAdminClient(connection) { client in
                try await client.call(.configSet, params: params, returning: ConfigSetResult.self)
            }
            var lines: [String] = []
            if !result.applied.isEmpty { lines.append("applied: \(result.applied.joined(separator: ", "))") }
            if !result.requiresRestart.isEmpty { lines.append("requires restart: \(result.requiresRestart.joined(separator: ", "))") }
            try Output.print(humanText: lines.joined(separator: "\n") + "\n", jsonValue: result, json: json)
        }
    }
```

> **Vérifier la signature de `client.call`** : `Get` utilise `client.call(.configGet, returning:)`
> (sans params). Les commandes avec params (ex. `RuleCommand.Add`) utilisent une
> surcharge `client.call(_:params:returning:)`. Si la surcharge avec `params:` n'existe
> pas exactement sous cette forme, l'aligner sur celle de `RuleCommands.swift` Add.

- [ ] **Step 2 : Test d'intégration CLI**

S'inspirer de `Tests/IntegrationTests/CLIConfigReloadTests.swift` (spawn d'un `irisd` éphémère). Créer `CLIConfigSetTests.swift` qui : démarre un daemon éphémère sur un `config.json` temporaire, lance `iris config set security.max_substitutions_per_minute 30`, puis `iris config get --json` et vérifie la valeur. (Reprendre le harness exact du fichier de référence — même mécanisme de socket/binaire.)

Run: `swift test --filter CLIConfigSetTests 2>&1 | tail -20`
Expected: PASS.

- [ ] **Step 3 : Commit**

```bash
git add -A
git commit -m "feat(phase-6.3a): CLI iris config set"
```

---

## Task 4 : Retirer la dépendance `TOMLKit`

**Files:**
- Modify: `Package.swift:27, 40`

- [ ] **Step 1 : Vérifier zéro usage TOML restant**

Run: `grep -rn "TOMLKit\|TOMLDecoder\|import TOMLKit\|ConfigLoader" Sources Tests`
Expected: aucune occurrence (ConfigLoader supprimé en Task 1 ; TOMLKit n'était importé que par lui).

- [ ] **Step 2 : Retirer la dépendance**

Dans `Package.swift`, supprimer la ligne 27 (`.package(url: "https://github.com/LebJe/TOMLKit.git", from: "0.6.0"),`) et la ligne 40 (`.product(name: "TOMLKit", package: "TOMLKit"),` dans les dépendances de la target IrisKit).

- [ ] **Step 3 : Résoudre + build + tests**

Run: `swift package resolve 2>&1 | tail -5`
Run: `swift build 2>&1 | tail -5` → succeeds
Run: `swift test 2>&1 | tail -10` → all pass

- [ ] **Step 4 : Commit**

```bash
git add -A
git commit -m "chore(phase-6.3a): retire la dépendance TOMLKit (plus de TOML)"
```

---

## Task 5 : Synchroniser SPECS.md + user-guide.md

**Files:**
- Modify: `SPECS.md` (§15.2-6, section config, arborescence §A, mentions TOML)
- Modify: `docs/user-guide.md` (section config, tableau d'état)

- [ ] **Step 1 : Amender SPECS.md**

- §15.2-6 (Settings) : remplacer « Listen ports (read-only display, edit via `config.toml` reload) » et les puces dérivées par la réalité 6.3a : réglages affichés via `config.get`, champs chauds éditables in-app via `config.set`, champs structurels en read-only avec « restart requis ».
- Section config : `config.toml` (TOML) → `config.json` (JSON, app-first, possédé par le daemon, seedé au 1er boot, backups `backups/` cap configurable).
- Arborescence §A : `config.toml` → `config.json` + dossier `backups/`. Retirer `runtime-rules.json`.
- Retirer toute mention de TOML/TOMLKit comme format de config.

- [ ] **Step 2 : Amender docs/user-guide.md**

- Section configuration : exemple TOML → exemple JSON (le schéma de la spec §4). Documenter `iris config set`, le seed automatique, les backups.
- Tableau d'état d'implémentation : marquer 6.3a (modèle config unifié) comme livré.

- [ ] **Step 3 : Commit**

```bash
git add -A
git commit -m "docs(phase-6.3a): synchronise SPECS + user-guide (config TOML→JSON app-first)"
```

---

## Task 6 : Vérification finale + smoke

**Files:** aucun (vérification).

- [ ] **Step 1 : Suite complète + lint**

Run: `swift build 2>&1 | tail -5` → succeeds, 0 warning
Run: `swift test 2>&1 | tail -15` → tous verts
Run: `swift-format lint --recursive Sources Tests 2>&1 | tail -5` → clean

- [ ] **Step 2 : Smoke daemon à froid (seed)**

```bash
rm -f "$HOME/Library/Application Support/iris/config.toml"   # n'existe pas, idempotent
mv "$HOME/Library/Application Support/iris/runtime-rules.json" /tmp/ 2>/dev/null || true
mv "$HOME/Library/Application Support/iris/config.json" /tmp/ 2>/dev/null || true
swift build -c release
.build/release/irisd --foreground &
sleep 2
ls -la "$HOME/Library/Application Support/iris/config.json"   # seedé, 0600
.build/release/iris config get
.build/release/iris config set security.on_exfil_attempt block_only
.build/release/iris config get | grep on_exfil_attempt        # == block_only
.build/release/iris config set backups.max_count 3
.build/release/iris rule add api.example.com
.build/release/iris rule list                                 # contient api.example.com
ls -la "$HOME/Library/Application Support/iris/backups/"      # backups horodatés, ≤ cap
kill %1
```

Expected : seed correct (0600), `config get/set` cohérents, `rule add/list` reflétés dans `config.json`, backups créés sous le cap. Restaurer les fichiers /tmp si besoin après smoke.

- [ ] **Step 3 : Smoke substitution `claude` (non-régression)**

Vérifier qu'une requête `claude` passe toujours par le proxy avec substitution (le cœur du produit n'a pas régressé après la refonte). Manuel, au poste.

- [ ] **Step 4 : Ouvrir la PR**

Pousser la branche et ouvrir la PR `feat/phase-6.3a-config-unifiee` → `main` avec la checklist de smoke testing (CLAUDE.md §8). Ne PAS merger sans validation Gemini + confirmation utilisateur.

---

## Self-Review (rempli par l'auteur du plan)

**Couverture spec ↔ tâches :**
- §3 ConfigStore acteur → T1.
- §4 schéma config.json (version/broker/security/backups/hosts) → T1 step 1-2.
- §5 RPC config.get/set/reload + rule.* + hot/restart → T1 (get/reload/rule), T2 (set).
- §6 seed 1er boot → T1 (`loadOrSeed`), testé `testSeedsWhenFileAbsent`.
- §7 backups + rotation + cap configurable → T1 (`backupCurrentFile`/`rotateBackups`), testé.
- §8 rupture/impact (suppr ConfigLoader/RuntimeRulesStore, TOMLKit, App path, SPECS) → T1 + T4 + T5.
- §9 erreurs (corrupted→throw, set invalide→intact, backup fail→abort) → T1/T2 tests.
- §10 tests → T1/T2/T3.
- §12 DoD → T6 smoke.

**Placeholders :** aucun « TBD/TODO » exécutable. Les deux points délégués à l'exécutant
(harness CLI de T3 step 2, adaptation de `ConfigReloadDispatcherTests` en T1 step 7)
pointent un fichier de référence précis à copier — pas une invention.

**Cohérence des types :** `ConfigStore.applyUpdates([ConfigSetParams.Update]) -> ConfigSetResult`
identique entre T2 step 2 (test), step 3 (impl), step 4 (dispatcher) et T3 (CLI).
`MITMRule(host:createdAt:source:)` (synthèse `.runtime`) cohérent T1. `Config.with(hosts:)`
défini T1 step 2, utilisé en tests.

**Risque connu signalé :** T1 laisse le build rouge entre les steps 1-7 (changement de
type central, inévitable) — un seul commit en step 9. Documenté en tête de T1.
