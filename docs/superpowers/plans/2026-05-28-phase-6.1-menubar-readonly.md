# Phase 6.1 — Menu bar app read-only Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Livrer `Iris.app` menu-bar accessory app read-only (3 onglets Overview/Logs/Security) qui consomme `block_and_notify` via SSE + RPC + `UNUserNotificationCenter`, sans nouvelle API daemon.

**Architecture:** Nouveau target SwiftPM `IrisAppCore` (library testable headless) contient `AppModel` (`@MainActor ObservableObject`) + `SyncCoordinator` (SSE primaire + RPC stats 5 s + reconnect exponentiel 1→30 s) + `NotificationBuilder` (pure). Nouvel Xcode project `IrisApp.xcodeproj` à la racine avec target unique `Iris.app` (`LSUIElement=YES`, bundle `io.iris.app`) qui linke IrisAppCore via SwiftPM local package et fournit NSStatusItem + NSPopover SwiftUI + `UNUserNotificationCenterDelegate`.

**Tech Stack:** Swift 5.9+ strict concurrency, SwiftUI macOS 13+, AppKit (NSStatusItem/NSPopover/NSHostingController), `UserNotifications`, Combine. Aucune nouvelle dépendance hors CLAUDE.md §1.

**Spec source :** `docs/superpowers/specs/2026-05-28-phase-6.1-menubar-readonly-design.md`

**Découpe en 2 PRs :**
- **PR 6.1a** (Tasks 1–14) : `IrisAppCore` SwiftPM library + tests, mergeable indépendamment, validable via `swift test` headless.
- **PR 6.1b** (Tasks 15–24) : Xcode project + UI SwiftUI + AppKit glue consommant IrisAppCore.

**Branches :**
- `feat/phase-6.1a-irisappcore` pour PR a
- `feat/phase-6.1b-menubar-ui` pour PR b (basée sur main après merge de a)

**Conventions de test** : XCTest synchrone quand possible, `async throws` quand on traverse l'`@MainActor` ou les protocoles client. `@MainActor` sur les méthodes de test qui touchent `AppModel`. Pas de `Task.sleep` — utiliser un `Clock` injectable.

---

# PR 6.1a — IrisAppCore (SwiftPM library)

## Task 1 : Branch + ajout cibles `IrisAppCore` / `IrisAppCoreTests` dans Package.swift

**Files:**
- Modify: `Package.swift`
- Create: `Sources/IrisAppCore/Placeholder.swift`
- Create: `Tests/IrisAppCoreTests/PlaceholderTests.swift`

Cette tâche scaffold les nouveaux targets sans logique, pour vérifier que `swift build` et `swift test` les voient.

- [ ] **Step 1: Créer la branche**

```bash
git checkout main && git pull
git checkout -b feat/phase-6.1a-irisappcore
```

- [ ] **Step 2: Ajouter les targets dans `Package.swift`**

Dans `Package.swift`, dans la section `products`, ajouter avant la dernière `]` :

```swift
        .library(name: "IrisAppCore", targets: ["IrisAppCore"]),
```

Dans la section `targets`, ajouter avant le dernier `]` (juste après le target `IntegrationTests`) :

```swift
        .target(
            name: "IrisAppCore",
            dependencies: [
                "IrisKit",
                .product(name: "Logging", package: "swift-log"),
            ],
            swiftSettings: strictConcurrency
        ),
        .testTarget(
            name: "IrisAppCoreTests",
            dependencies: [
                "IrisAppCore",
                "IrisKit",
            ],
            swiftSettings: strictConcurrency
        ),
```

- [ ] **Step 3: Créer le fichier placeholder pour que SwiftPM accepte les targets**

`Sources/IrisAppCore/Placeholder.swift` :

```swift
import Foundation

/// Replaced by real public API in subsequent tasks.
public enum IrisAppCorePlaceholder { case unused }
```

`Tests/IrisAppCoreTests/PlaceholderTests.swift` :

```swift
import XCTest
@testable import IrisAppCore

final class PlaceholderTests: XCTestCase {
    func testPlaceholderExists() {
        XCTAssertNotNil(IrisAppCorePlaceholder.unused)
    }
}
```

- [ ] **Step 4: Vérifier que tout compile et que le test passe**

Run: `swift build && swift test --filter PlaceholderTests`
Expected: build clean, 1 test passes.

- [ ] **Step 5: Commit**

```bash
git add Package.swift Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "chore(phase-6.1a): add IrisAppCore + IrisAppCoreTests targets"
```

---

## Task 2 : `DaemonStatus` enum + `LogFilters` struct

**Files:**
- Create: `Sources/IrisAppCore/Models/DaemonStatus.swift`
- Create: `Sources/IrisAppCore/Models/LogFilters.swift`
- Create: `Tests/IrisAppCoreTests/Models/LogFiltersTests.swift`

`DaemonStatus` est une `enum` simple sans logique testable — pas de tests dédiés. `LogFilters` porte la logique de filtrage local, testable.

- [ ] **Step 1: Écrire les tests `LogFilters` (failing)**

`Tests/IrisAppCoreTests/Models/LogFiltersTests.swift` :

```swift
import XCTest
import IrisKit
@testable import IrisAppCore

final class LogFiltersTests: XCTestCase {
    private func makeEvent(
        kind: Event.Kind,
        host: String,
        path: String = "/v1/x",
        secret: String? = nil
    ) -> Event {
        Event(
            timestamp: Date(),
            kind: kind,
            host: host,
            method: "GET",
            path: path,
            substitutedSecrets: secret.map { [$0] } ?? []
        )
    }

    func testEmptyFiltersPassThrough() {
        let filters = LogFilters()
        let event = makeEvent(kind: .substituted, host: "api.anthropic.com")
        XCTAssertTrue(filters.matches(event))
    }

    func testKindFilterMatchesOnlyListed() {
        var filters = LogFilters()
        filters.kinds = [.exfilBlocked]
        XCTAssertFalse(filters.matches(makeEvent(kind: .substituted, host: "x.com")))
        XCTAssertTrue(filters.matches(makeEvent(kind: .exfilBlocked, host: "x.com")))
    }

    func testHostFilterCaseInsensitiveSubstring() {
        var filters = LogFilters()
        filters.host = "ANTHROPIC"
        XCTAssertTrue(filters.matches(makeEvent(kind: .substituted, host: "api.anthropic.com")))
        XCTAssertFalse(filters.matches(makeEvent(kind: .substituted, host: "api.github.com")))
    }

    func testSearchMatchesPathHostOrSecretName() {
        var filters = LogFilters()
        filters.search = "messages"
        XCTAssertTrue(
            filters.matches(makeEvent(kind: .substituted, host: "x.com", path: "/v1/messages"))
        )
        XCTAssertFalse(
            filters.matches(makeEvent(kind: .substituted, host: "x.com", path: "/v1/chat"))
        )
        filters.search = "anthropic_api_key"
        XCTAssertTrue(
            filters.matches(
                makeEvent(kind: .substituted, host: "x.com", secret: "anthropic_api_key")
            )
        )
    }

    func testFiltersAreANDed() {
        var filters = LogFilters()
        filters.kinds = [.substituted]
        filters.host = "anthropic"
        let match = makeEvent(kind: .substituted, host: "api.anthropic.com")
        let kindMismatch = makeEvent(kind: .exfilBlocked, host: "api.anthropic.com")
        let hostMismatch = makeEvent(kind: .substituted, host: "api.github.com")
        XCTAssertTrue(filters.matches(match))
        XCTAssertFalse(filters.matches(kindMismatch))
        XCTAssertFalse(filters.matches(hostMismatch))
    }
}
```

- [ ] **Step 2: Run tests, expect compile failure**

Run: `swift test --filter LogFiltersTests`
Expected: compile error `cannot find type 'LogFilters' in scope`.

- [ ] **Step 3: Implémenter `DaemonStatus` + `LogFilters`**

`Sources/IrisAppCore/Models/DaemonStatus.swift` :

```swift
import Foundation
import IrisKit

public enum DaemonStatus: Sendable, Equatable {
    case connecting
    case up(stats: DaemonStats, uptime: TimeInterval, paused: Bool)
    case down(reason: DownReason)

    public enum DownReason: Sendable, Equatable {
        case notRunning
        case rpcError(String)
    }

    public var isUp: Bool {
        if case .up = self { return true }
        return false
    }
}
```

`Sources/IrisAppCore/Models/LogFilters.swift` :

```swift
import Foundation
import IrisKit

public struct LogFilters: Sendable, Equatable {
    public var kinds: Set<Event.Kind>
    public var host: String
    public var search: String

    public init(kinds: Set<Event.Kind> = [], host: String = "", search: String = "") {
        self.kinds = kinds
        self.host = host
        self.search = search
    }

    public func matches(_ event: Event) -> Bool {
        if !kinds.isEmpty && !kinds.contains(event.kind) { return false }
        let hostQuery = host.trimmingCharacters(in: .whitespaces)
        if !hostQuery.isEmpty,
            event.host.range(of: hostQuery, options: .caseInsensitive) == nil
        {
            return false
        }
        let needle = search.trimmingCharacters(in: .whitespaces)
        if !needle.isEmpty {
            let hayHost = event.host.range(of: needle, options: .caseInsensitive) != nil
            let hayPath = event.path.range(of: needle, options: .caseInsensitive) != nil
            let haySecret = event.substitutedSecrets.contains {
                $0.range(of: needle, options: .caseInsensitive) != nil
            }
            if !(hayHost || hayPath || haySecret) { return false }
        }
        return true
    }
}
```

- [ ] **Step 4: Run tests, verify pass**

Run: `swift test --filter LogFiltersTests`
Expected: 5 tests pass.

- [ ] **Step 5: Commit**

```bash
git add Sources/IrisAppCore/Models Tests/IrisAppCoreTests/Models
git commit -m "feat(phase-6.1a): DaemonStatus enum + LogFilters predicate"
```

---

## Task 3 : Protocoles `AdminCalling` + `EventsSubscribing` + extensions IrisKit

**Files:**
- Create: `Sources/IrisAppCore/Protocols/AdminCalling.swift`
- Create: `Sources/IrisAppCore/Protocols/EventsSubscribing.swift`
- Create: `Sources/IrisAppCore/Protocols/IrisKitConformances.swift`

Pour rendre `SyncCoordinator` testable avec des mocks, on abstrait les deux clients IrisKit derrière des protocoles `Sendable`. La conformance des types IrisKit concrets vit dans IrisAppCore (pas de modification de IrisKit). Le scope du protocole est strictement réduit aux appels effectivement utilisés par `SyncCoordinator` (YAGNI).

- [ ] **Step 1: Créer les protocoles**

`Sources/IrisAppCore/Protocols/AdminCalling.swift` :

```swift
import Foundation
import IrisKit

public protocol AdminCalling: Sendable {
    func fetchStatus() async throws -> IrisKit.DaemonStatus
    func fetchStats() async throws -> DaemonStats
    func pause() async throws
    func resume() async throws
    func queryEvents(since: Date?, limit: Int?) async throws -> [Event]
}
```

`Sources/IrisAppCore/Protocols/EventsSubscribing.swift` :

```swift
import Foundation
import IrisKit

public protocol EventsSubscribing: Sendable {
    func subscribe(since: Date?) async throws -> AsyncThrowingStream<EventsClientItem, Error>
}
```

- [ ] **Step 2: Implémenter les conformances IrisKit**

`Sources/IrisAppCore/Protocols/IrisKitConformances.swift` :

```swift
import Foundation
import IrisKit

extension AdminClient: AdminCalling {
    public func fetchStatus() async throws -> IrisKit.DaemonStatus {
        try await call(.daemonStatus, returning: IrisKit.DaemonStatus.self)
    }

    public func fetchStats() async throws -> DaemonStats {
        try await call(.daemonStats, returning: DaemonStats.self)
    }

    public func pause() async throws {
        _ = try await call(.daemonPause, returning: DaemonPauseResult.self)
    }

    public func resume() async throws {
        _ = try await call(.daemonResume, returning: DaemonPauseResult.self)
    }

    public func queryEvents(since: Date?, limit: Int?) async throws -> [Event] {
        try await call(
            .eventsQuery,
            params: EventsQueryParams(since: since, until: nil, limit: limit, kind: nil, host: nil),
            returning: [Event].self
        )
    }
}

extension EventsClient: EventsSubscribing {
    public func subscribe(since: Date?) async throws -> AsyncThrowingStream<EventsClientItem, Error> {
        try await subscribe(since: since, kinds: nil, host: nil)
    }
}
```

**Note** : `fetchStatus` retourne `IrisKit.DaemonStatus` (le DTO RPC), pas le `DaemonStatus` de IrisAppCore (la machine à états UI). Les deux types coexistent ; le mapping se fait dans `SyncCoordinator` (Task 7).

- [ ] **Step 3: Vérifier que ça compile**

Run: `swift build`
Expected: build clean.

- [ ] **Step 4: Commit**

```bash
git add Sources/IrisAppCore/Protocols
git commit -m "feat(phase-6.1a): AdminCalling + EventsSubscribing protocols with IrisKit conformances"
```

---

## Task 4 : `AppModel` skeleton (state + onglet persisté + acknowledgment)

**Files:**
- Create: `Sources/IrisAppCore/AppModel.swift`
- Create: `Tests/IrisAppCoreTests/AppModelTests.swift`

Squelette de `AppModel` avec champs `@Published`, persistance UserDefaults pour `selectedTab` et `lastAcknowledgedAt`. Pas encore d'event ingestion (Task 5).

- [ ] **Step 1: Écrire les tests `AppModel` initial state + persistence**

`Tests/IrisAppCoreTests/AppModelTests.swift` :

```swift
import XCTest
import IrisKit
@testable import IrisAppCore

@MainActor
final class AppModelTests: XCTestCase {
    private var defaults: UserDefaults!

    override func setUp() async throws {
        defaults = UserDefaults(suiteName: "io.iris.app.test.\(UUID().uuidString)")!
    }

    override func tearDown() async throws {
        defaults.removePersistentDomain(forName: defaults.dictionaryRepresentation().keys.first ?? "")
    }

    func testInitialState() {
        let model = AppModel(defaults: defaults)
        XCTAssertEqual(model.daemonStatus, .connecting)
        XCTAssertTrue(model.events.isEmpty)
        XCTAssertTrue(model.alerts.isEmpty)
        XCTAssertEqual(model.unreadAlertCount, 0)
        XCTAssertFalse(model.streamPaused)
        XCTAssertEqual(model.logFilters, LogFilters())
        XCTAssertNil(model.focusedAlertID)
        XCTAssertEqual(model.selectedTab, .overview)
    }

    func testSelectedTabPersistsAcrossInstances() {
        let model1 = AppModel(defaults: defaults)
        model1.selectedTab = .security
        let model2 = AppModel(defaults: defaults)
        XCTAssertEqual(model2.selectedTab, .security)
    }

    func testLastAcknowledgedAtPersists() {
        let when = Date(timeIntervalSince1970: 1_700_000_000)
        let model1 = AppModel(defaults: defaults)
        model1.markAllAlertsRead(now: when)
        let model2 = AppModel(defaults: defaults)
        XCTAssertEqual(model2.lastAcknowledgedAt, when)
    }
}
```

- [ ] **Step 2: Run tests, expect compile failure**

Run: `swift test --filter AppModelTests`
Expected: compile error `cannot find type 'AppModel' in scope`.

- [ ] **Step 3: Implémenter `AppModel` skeleton**

`Sources/IrisAppCore/AppModel.swift` :

```swift
import Combine
import Foundation
import IrisKit

@MainActor
public final class AppModel: ObservableObject {
    public enum Tab: String, Sendable, CaseIterable, Codable {
        case overview, logs, security
    }

    @Published public var daemonStatus: DaemonStatus = .connecting
    @Published public var events: [Event] = []
    @Published public var alerts: [Event] = []
    @Published public var unreadAlertCount: Int = 0
    @Published public var streamPaused: Bool = false
    @Published public var logFilters: LogFilters = LogFilters()
    @Published public var focusedAlertID: UUID?
    @Published public var notificationsEnabled: Bool = true
    @Published public var selectedTab: Tab {
        didSet { defaults.set(selectedTab.rawValue, forKey: Self.tabKey) }
    }
    @Published public private(set) var lastAcknowledgedAt: Date?

    private let defaults: UserDefaults
    private static let tabKey = "io.iris.app.selectedTab"
    private static let ackKey = "io.iris.app.lastAcknowledgedAt"

    /// Max in-memory events ring size.
    public static let eventsCap: Int = 1000
    /// Max in-memory alerts ring size.
    public static let alertsCap: Int = 200

    public init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
        let storedTab = defaults.string(forKey: Self.tabKey).flatMap(Tab.init(rawValue:))
        self.selectedTab = storedTab ?? .overview
        if let ts = defaults.object(forKey: Self.ackKey) as? Date {
            self.lastAcknowledgedAt = ts
        }
    }

    public func markAllAlertsRead(now: Date = Date()) {
        lastAcknowledgedAt = now
        defaults.set(now, forKey: Self.ackKey)
        recomputeUnreadCount()
    }

    public func acknowledgeAlert(eventID: UUID) {
        guard let evt = alerts.first(where: { $0.id == eventID }) else { return }
        let bound = max(lastAcknowledgedAt ?? .distantPast, evt.timestamp)
        lastAcknowledgedAt = bound
        defaults.set(bound, forKey: Self.ackKey)
        recomputeUnreadCount()
    }

    func recomputeUnreadCount() {
        guard let cutoff = lastAcknowledgedAt else {
            unreadAlertCount = alerts.count
            return
        }
        unreadAlertCount = alerts.filter { $0.timestamp > cutoff }.count
    }
}
```

- [ ] **Step 4: Run tests, verify pass**

Run: `swift test --filter AppModelTests`
Expected: 3 tests pass.

- [ ] **Step 5: Commit**

```bash
git add Sources/IrisAppCore/AppModel.swift Tests/IrisAppCoreTests/AppModelTests.swift
git commit -m "feat(phase-6.1a): AppModel skeleton with UserDefaults persistence"
```

---

## Task 5 : `AppModel` event ingestion + dédup + ring cap

**Files:**
- Modify: `Sources/IrisAppCore/AppModel.swift`
- Modify: `Tests/IrisAppCoreTests/AppModelTests.swift`

Méthode `ingest(_ event:)` qui append en tête, dédup par UUID, et cap à `eventsCap` / `alertsCap`. Met à jour `alerts` et `unreadAlertCount`.

- [ ] **Step 1: Ajouter les tests d'ingestion**

Ajouter à la fin de `AppModelTests` (avant la dernière `}` de classe) :

```swift
    private func makeEvent(
        id: UUID = UUID(),
        timestamp: Date = Date(),
        kind: Event.Kind = .substituted,
        host: String = "api.example.com"
    ) -> Event {
        Event(id: id, timestamp: timestamp, kind: kind, host: host, method: "GET", path: "/x")
    }

    private func makeAlertEvent(
        timestamp: Date = Date(),
        severity: Alert.Severity = .high
    ) -> Event {
        let alert = Alert(
            severity: severity,
            rule: .hostMismatch,
            secretName: "anthropic_api_key",
            detectedAt: .header,
            snippet: "x-api-key: {{kc:anthropic_api_key}}"
        )
        return Event(
            timestamp: timestamp,
            kind: .exfilBlocked,
            host: "api.github.com",
            method: "POST",
            path: "/issues",
            alert: alert
        )
    }

    func testIngestAppendsNewestFirst() {
        let model = AppModel(defaults: defaults)
        let older = makeEvent(timestamp: Date(timeIntervalSince1970: 1_000))
        let newer = makeEvent(timestamp: Date(timeIntervalSince1970: 2_000))
        model.ingest(older)
        model.ingest(newer)
        XCTAssertEqual(model.events.map(\.id), [newer.id, older.id])
    }

    func testIngestDedupsByUUID() {
        let model = AppModel(defaults: defaults)
        let id = UUID()
        let e1 = makeEvent(id: id, timestamp: Date(timeIntervalSince1970: 1_000))
        let e2 = makeEvent(id: id, timestamp: Date(timeIntervalSince1970: 2_000))
        model.ingest(e1)
        model.ingest(e2)
        XCTAssertEqual(model.events.count, 1)
        XCTAssertEqual(model.events[0].id, id)
    }

    func testIngestRespectsCap() {
        let model = AppModel(defaults: defaults)
        for i in 0..<(AppModel.eventsCap + 50) {
            model.ingest(makeEvent(timestamp: Date(timeIntervalSince1970: Double(i))))
        }
        XCTAssertEqual(model.events.count, AppModel.eventsCap)
    }

    func testExfilEventIsAlsoAddedToAlerts() {
        let model = AppModel(defaults: defaults)
        let evt = makeAlertEvent()
        model.ingest(evt)
        XCTAssertEqual(model.events.count, 1)
        XCTAssertEqual(model.alerts.count, 1)
        XCTAssertEqual(model.alerts[0].id, evt.id)
    }

    func testUnreadCountIncrementsForNewAlerts() {
        let model = AppModel(defaults: defaults)
        model.markAllAlertsRead(now: Date(timeIntervalSince1970: 1_000))
        let newAlert = makeAlertEvent(timestamp: Date(timeIntervalSince1970: 2_000))
        model.ingest(newAlert)
        XCTAssertEqual(model.unreadAlertCount, 1)
    }

    func testUnreadCountIgnoresAcknowledgedAlerts() {
        let model = AppModel(defaults: defaults)
        let oldAlert = makeAlertEvent(timestamp: Date(timeIntervalSince1970: 1_000))
        model.ingest(oldAlert)
        model.markAllAlertsRead(now: Date(timeIntervalSince1970: 2_000))
        XCTAssertEqual(model.unreadAlertCount, 0)
    }
```

- [ ] **Step 2: Run tests, expect compile failure**

Run: `swift test --filter AppModelTests`
Expected: compile error `value of type 'AppModel' has no member 'ingest'`.

- [ ] **Step 3: Implémenter `ingest`**

Dans `Sources/IrisAppCore/AppModel.swift`, ajouter avant la dernière `}` de classe :

```swift
    public func ingest(_ event: Event) {
        ingestInto(&events, event: event, cap: Self.eventsCap)
        if event.kind == .exfilBlocked {
            ingestInto(&alerts, event: event, cap: Self.alertsCap)
            recomputeUnreadCount()
        }
    }

    public func ingestBatch(_ batch: [Event]) {
        for event in batch { ingest(event) }
    }

    private func ingestInto(_ ring: inout [Event], event: Event, cap: Int) {
        if let idx = ring.firstIndex(where: { $0.id == event.id }) {
            ring[idx] = event
            return
        }
        ring.insert(event, at: 0)
        if ring.count > cap {
            ring.removeLast(ring.count - cap)
        }
    }
```

- [ ] **Step 4: Run tests, verify pass**

Run: `swift test --filter AppModelTests`
Expected: 9 tests pass (3 from Task 4 + 6 new).

- [ ] **Step 5: Commit**

```bash
git add Sources/IrisAppCore/AppModel.swift Tests/IrisAppCoreTests/AppModelTests.swift
git commit -m "feat(phase-6.1a): AppModel.ingest with UUID dedup, ring cap, alerts derivation"
```

---

## Task 6 : `AppModel` pause/resume action wiring + last event timestamp helper

**Files:**
- Modify: `Sources/IrisAppCore/AppModel.swift`
- Modify: `Tests/IrisAppCoreTests/AppModelTests.swift`

Méthode `togglePause(via:)` qui appelle l'`AdminCalling` et met à jour `daemonStatus.paused`. Helper `lastEventTimestamp` utilisé par le coordinator pour `since:`.

- [ ] **Step 1: Ajouter les tests**

Ajouter à la fin de `AppModelTests` :

```swift
    func testLastEventTimestampReturnsNewestSeen() {
        let model = AppModel(defaults: defaults)
        XCTAssertNil(model.lastEventTimestamp)
        model.ingest(makeEvent(timestamp: Date(timeIntervalSince1970: 1_000)))
        model.ingest(makeEvent(timestamp: Date(timeIntervalSince1970: 3_000)))
        model.ingest(makeEvent(timestamp: Date(timeIntervalSince1970: 2_000)))
        XCTAssertEqual(model.lastEventTimestamp, Date(timeIntervalSince1970: 3_000))
    }

    func testTogglePauseInvokesAdminPauseWhenUp() async throws {
        let model = AppModel(defaults: defaults)
        model.daemonStatus = .up(stats: .zero, uptime: 0, paused: false)
        let admin = FakeAdminCalling()
        try await model.togglePause(via: admin)
        XCTAssertEqual(admin.calls, ["pause"])
        XCTAssertEqual(
            model.daemonStatus,
            .up(stats: .zero, uptime: 0, paused: true)
        )
    }

    func testTogglePauseInvokesAdminResumeWhenPaused() async throws {
        let model = AppModel(defaults: defaults)
        model.daemonStatus = .up(stats: .zero, uptime: 0, paused: true)
        let admin = FakeAdminCalling()
        try await model.togglePause(via: admin)
        XCTAssertEqual(admin.calls, ["resume"])
        XCTAssertEqual(
            model.daemonStatus,
            .up(stats: .zero, uptime: 0, paused: false)
        )
    }
```

Et créer le mock dans `Tests/IrisAppCoreTests/Mocks/FakeAdminCalling.swift` :

```swift
import Foundation
import IrisKit
@testable import IrisAppCore

final class FakeAdminCalling: AdminCalling, @unchecked Sendable {
    var calls: [String] = []
    var stubStatus: IrisKit.DaemonStatus = IrisKit.DaemonStatus(
        pid: 1, uptimeS: 0, version: "test", stats: .zero
    )
    var stubStats: DaemonStats = .zero
    var stubEvents: [Event] = []
    var shouldThrow: Error?

    func fetchStatus() async throws -> IrisKit.DaemonStatus {
        calls.append("status")
        if let e = shouldThrow { throw e }
        return stubStatus
    }

    func fetchStats() async throws -> DaemonStats {
        calls.append("stats")
        if let e = shouldThrow { throw e }
        return stubStats
    }

    func pause() async throws {
        calls.append("pause")
        if let e = shouldThrow { throw e }
    }

    func resume() async throws {
        calls.append("resume")
        if let e = shouldThrow { throw e }
    }

    func queryEvents(since: Date?, limit: Int?) async throws -> [Event] {
        calls.append("queryEvents(since:\(since?.timeIntervalSince1970 ?? -1),limit:\(limit ?? -1))")
        if let e = shouldThrow { throw e }
        return stubEvents
    }
}
```

- [ ] **Step 2: Run tests, expect compile failure**

Run: `swift test --filter AppModelTests`
Expected: `lastEventTimestamp` and `togglePause(via:)` missing.

- [ ] **Step 3: Implémenter**

Dans `Sources/IrisAppCore/AppModel.swift`, ajouter :

```swift
    public var lastEventTimestamp: Date? {
        events.map(\.timestamp).max()
    }

    public func togglePause(via admin: AdminCalling) async throws {
        let wasPaused: Bool
        switch daemonStatus {
        case .up(_, _, let paused): wasPaused = paused
        default: return
        }
        if wasPaused {
            try await admin.resume()
        } else {
            try await admin.pause()
        }
        if case .up(let s, let u, _) = daemonStatus {
            daemonStatus = .up(stats: s, uptime: u, paused: !wasPaused)
        }
    }
```

- [ ] **Step 4: Run tests, verify pass**

Run: `swift test --filter AppModelTests`
Expected: 12 tests pass.

- [ ] **Step 5: Commit**

```bash
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.1a): AppModel.togglePause + lastEventTimestamp helper"
```

---

## Task 7 : `SyncCoordinator` bootstrap (initial snapshot, no SSE yet)

**Files:**
- Create: `Sources/IrisAppCore/SyncCoordinator.swift`
- Create: `Tests/IrisAppCoreTests/SyncCoordinatorTests.swift`
- Create: `Tests/IrisAppCoreTests/Mocks/FakeEventsSubscribing.swift`

Phase 1 du coordinator : bootstrap = `fetchStatus` → si up → `queryEvents(since: nil, limit: 100)` → `ingestBatch` → `daemonStatus = .up`. Pas encore de SSE ni de retry. Le mapping `IrisKit.DaemonStatus → DaemonStatus.up` se fait ici.

- [ ] **Step 1: Créer le mock EventsSubscribing**

`Tests/IrisAppCoreTests/Mocks/FakeEventsSubscribing.swift` :

```swift
import Foundation
import IrisKit
@testable import IrisAppCore

/// AsyncThrowingStream that you can push items into from tests.
///
/// Pre-buffering : `push` and `finish` called BEFORE the first `subscribe`
/// are queued and replayed at subscribe-time, so tests don't need to manage
/// timing between Task creation and consumer registration.
///
/// `autoFinishCount` : number of subscribes that should immediately emit a
/// finish event after returning the stream — used to model rapid SSE drops
/// in reconnect tests without juggling background tasks.
final class FakeEventsSubscribing: EventsSubscribing, @unchecked Sendable {
    var subscribedSince: [Date?] = []
    var subscribeError: Error?
    var autoFinishCount: Int = 0

    private var pending: [AsyncThrowingStream<EventsClientItem, Error>.Continuation] = []
    private var preBuffered: [EventsClientItem] = []
    private var preBufferedFinish: Error??

    func push(_ item: EventsClientItem) {
        if let last = pending.last {
            last.yield(item)
        } else {
            preBuffered.append(item)
        }
    }

    func finish(throwing error: Error? = nil) {
        if let last = pending.last {
            last.finish(throwing: error)
        } else {
            preBufferedFinish = error  // Encodes "finish queued" via outer Optional non-nil.
        }
    }

    func subscribe(since: Date?) async throws -> AsyncThrowingStream<EventsClientItem, Error> {
        subscribedSince.append(since)
        if let e = subscribeError { throw e }
        let (stream, cont) = AsyncThrowingStream<EventsClientItem, Error>.makeStream()
        pending.append(cont)
        for item in preBuffered { cont.yield(item) }
        preBuffered.removeAll()
        if let optErr = preBufferedFinish {
            cont.finish(throwing: optErr)
            preBufferedFinish = nil
        } else if autoFinishCount > 0 {
            autoFinishCount -= 1
            cont.finish()
        }
        return stream
    }
}
```

- [ ] **Step 2: Écrire les tests bootstrap**

`Tests/IrisAppCoreTests/SyncCoordinatorTests.swift` :

```swift
import XCTest
import IrisKit
@testable import IrisAppCore

@MainActor
final class SyncCoordinatorTests: XCTestCase {
    func testBootstrapWithDaemonUpFetchesStatusAndEvents() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        admin.stubStatus = IrisKit.DaemonStatus(
            pid: 42, uptimeS: 100, version: "test",
            stats: DaemonStats(reqTotal: 5, subTotal: 3, exfilBlockedTotal: 0, errorsTotal: 0)
        )
        admin.stubEvents = [
            Event(timestamp: Date(timeIntervalSince1970: 1), kind: .substituted, host: "x.com",
                  method: "GET", path: "/")
        ]
        let events = FakeEventsSubscribing()
        let coord = SyncCoordinator(model: model, admin: admin, events: events)

        try await coord.bootstrap()

        XCTAssertEqual(admin.calls, ["status", "queryEvents(since:-1.0,limit:100)"])
        XCTAssertEqual(model.events.count, 1)
        if case .up(let stats, let uptime, let paused) = model.daemonStatus {
            XCTAssertEqual(stats.reqTotal, 5)
            XCTAssertEqual(uptime, 100)
            XCTAssertFalse(paused)
        } else {
            XCTFail("expected .up, got \(model.daemonStatus)")
        }
    }

    func testBootstrapWithDaemonDownSetsDownState() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        admin.shouldThrow = AdminClientError.connectFailed
        let events = FakeEventsSubscribing()
        let coord = SyncCoordinator(model: model, admin: admin, events: events)

        try await coord.bootstrap()

        XCTAssertEqual(model.daemonStatus, .down(reason: .notRunning))
        XCTAssertTrue(model.events.isEmpty)
    }
}
```

- [ ] **Step 3: Run tests, expect compile failure**

Run: `swift test --filter SyncCoordinatorTests`
Expected: `cannot find type 'SyncCoordinator'`.

- [ ] **Step 4: Implémenter `SyncCoordinator.bootstrap()`**

`Sources/IrisAppCore/SyncCoordinator.swift` :

```swift
import Foundation
import IrisKit
import Logging

@MainActor
public final class SyncCoordinator {
    private let model: AppModel
    private let admin: AdminCalling
    private let events: EventsSubscribing
    private let logger: Logger

    public init(
        model: AppModel,
        admin: AdminCalling,
        events: EventsSubscribing,
        logger: Logger = Logger(label: "io.iris.app.sync")
    ) {
        self.model = model
        self.admin = admin
        self.events = events
        self.logger = logger
    }

    /// Initial fetch: probe daemon, snapshot stats + recent events. Idempotent.
    public func bootstrap() async throws {
        do {
            let status = try await admin.fetchStatus()
            let recent = try await admin.queryEvents(since: nil, limit: 100)
            model.daemonStatus = .up(
                stats: status.stats,
                uptime: TimeInterval(status.uptimeS),
                paused: false  // SPECS §15.4 — pause state isn't in daemon.status; infer false on bootstrap
            )
            model.ingestBatch(recent.reversed())  // oldest first → newest will end up at head
        } catch is AdminClientError {
            model.daemonStatus = .down(reason: .notRunning)
        } catch {
            model.daemonStatus = .down(reason: .rpcError(String(describing: error)))
        }
    }
}
```

- [ ] **Step 5: Run tests, verify pass**

Run: `swift test --filter SyncCoordinatorTests`
Expected: 2 tests pass.

- [ ] **Step 6: Commit**

```bash
git add Sources/IrisAppCore/SyncCoordinator.swift Tests/IrisAppCoreTests
git commit -m "feat(phase-6.1a): SyncCoordinator.bootstrap (snapshot status + events)"
```

---

## Task 8 : `SyncCoordinator` SSE consumption + `since:` continuation

**Files:**
- Modify: `Sources/IrisAppCore/SyncCoordinator.swift`
- Modify: `Tests/IrisAppCoreTests/SyncCoordinatorTests.swift`

Lance une `Task` SSE qui consomme `subscribe(since:)` et appelle `model.ingest(_:)` sur chaque `.event`. Le `since:` initial = `model.lastEventTimestamp ?? (now - 60s)`. Les `.ping` sont ignorés (gardent la connexion vivante côté URLSession).

- [ ] **Step 1: Ajouter les tests SSE**

Dans `SyncCoordinatorTests`, ajouter :

```swift
    func testSSEStreamIngestsEvents() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        let events = FakeEventsSubscribing()
        let coord = SyncCoordinator(model: model, admin: admin, events: events)

        try await coord.bootstrap()
        let evt = Event(
            timestamp: Date(timeIntervalSince1970: 100), kind: .substituted,
            host: "api.example.com", method: "GET", path: "/x"
        )
        // Pre-buffer event and finish — replayed when runStream calls subscribe.
        events.push(.event(evt))
        events.finish()
        try await coord.runStream()

        XCTAssertEqual(model.events.count, 1)
        XCTAssertEqual(model.events[0].id, evt.id)
    }

    func testSSESubscribeUsesLastEventTimestampWhenAvailable() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        let preExisting = Event(
            timestamp: Date(timeIntervalSince1970: 500), kind: .substituted,
            host: "x.com", method: "GET", path: "/"
        )
        admin.stubEvents = [preExisting]
        let events = FakeEventsSubscribing()
        let coord = SyncCoordinator(model: model, admin: admin, events: events)

        try await coord.bootstrap()
        events.finish()  // pre-buffered → consumed when runStream subscribes
        try await coord.runStream()

        XCTAssertEqual(events.subscribedSince, [Date(timeIntervalSince1970: 500)])
    }

    func testSSEPingItemsAreIgnoredAndDoNotCrash() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        let events = FakeEventsSubscribing()
        let coord = SyncCoordinator(model: model, admin: admin, events: events)

        try await coord.bootstrap()
        events.push(.ping)
        events.push(.ping)
        events.finish()
        try await coord.runStream()

        XCTAssertTrue(model.events.isEmpty)
    }
```

- [ ] **Step 2: Implémenter `runStream()`**

Dans `Sources/IrisAppCore/SyncCoordinator.swift`, ajouter avant la dernière `}` :

```swift
    /// Consume SSE stream until the underlying stream finishes or is cancelled.
    /// Caller is responsible for relaunching on error (Task 9 handles backoff).
    public func runStream() async throws {
        let since = model.lastEventTimestamp ?? Date(timeIntervalSinceNow: -60)
        let stream = try await events.subscribe(since: since)
        for try await item in stream {
            switch item {
            case .event(let event):
                model.ingest(event)
            case .ping:
                continue
            }
        }
    }
```

- [ ] **Step 3: Run tests, verify pass**

Run: `swift test --filter SyncCoordinatorTests`
Expected: 5 tests pass.

- [ ] **Step 4: Commit**

```bash
git add Sources/IrisAppCore/SyncCoordinator.swift Tests/IrisAppCoreTests/SyncCoordinatorTests.swift
git commit -m "feat(phase-6.1a): SyncCoordinator.runStream consumes SSE with since: continuation"
```

---

## Task 9 : `SyncCoordinator` reconnect avec backoff exponentiel (Clock-injected)

**Files:**
- Modify: `Sources/IrisAppCore/SyncCoordinator.swift`
- Modify: `Tests/IrisAppCoreTests/SyncCoordinatorTests.swift`
- Create: `Sources/IrisAppCore/Clock/AsyncSleeper.swift`
- Create: `Tests/IrisAppCoreTests/Mocks/FakeSleeper.swift`

Méthode `runStreamWithReconnect()` qui boucle sur `runStream()` + backoff 1→2→4→8→16→30 s. Le sleep est délégué à un `AsyncSleeper` injectable pour ne pas attendre réellement en test.

Sémantique : après chaque drop SSE, on incrémente le retry counter. À chaque retry on appelle `fetchStatus()` ; si OK → reset counter, re-subscribe ; sinon → continue de backoff. Après 3 échecs consécutifs de `fetchStatus` → `daemonStatus = .down`.

- [ ] **Step 1: Créer `AsyncSleeper` protocol + impl prod**

`Sources/IrisAppCore/Clock/AsyncSleeper.swift` :

```swift
import Foundation

public protocol AsyncSleeper: Sendable {
    func sleep(seconds: Double) async throws
}

public struct SystemSleeper: AsyncSleeper {
    public init() {}
    public func sleep(seconds: Double) async throws {
        try await Task.sleep(nanoseconds: UInt64(seconds * 1_000_000_000))
    }
}
```

- [ ] **Step 2: Créer `FakeSleeper` mock**

`Tests/IrisAppCoreTests/Mocks/FakeSleeper.swift` :

```swift
import Foundation
@testable import IrisAppCore

final class FakeSleeper: AsyncSleeper, @unchecked Sendable {
    private(set) var delays: [Double] = []
    func sleep(seconds: Double) async throws {
        delays.append(seconds)
    }
}
```

- [ ] **Step 3: Écrire le test du backoff progression**

Ajouter à `SyncCoordinatorTests` :

```swift
    func testReconnectBackoffFollowsExponentialSequence() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        // Both transports broken → backoff grows monotonically (no reset path hit).
        admin.shouldThrow = AdminClientError.connectFailed
        let events = FakeEventsSubscribing()
        events.autoFinishCount = 100  // every subscribe yields an empty, finished stream
        let sleeper = FakeSleeper()
        let coord = SyncCoordinator(model: model, admin: admin, events: events, sleeper: sleeper)

        try await coord.bootstrap()
        try await coord.runStreamWithReconnect(maxAttempts: 6)

        // 6 attempts → 6 sleeps observed (one per attempt after stream end).
        XCTAssertEqual(sleeper.delays.prefix(5).map { $0 }, [1, 2, 4, 8, 16])
    }

    func testReconnectCapsAt30Seconds() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        admin.shouldThrow = AdminClientError.connectFailed
        let events = FakeEventsSubscribing()
        events.autoFinishCount = 100
        let sleeper = FakeSleeper()
        let coord = SyncCoordinator(model: model, admin: admin, events: events, sleeper: sleeper)

        try await coord.bootstrap()
        try await coord.runStreamWithReconnect(maxAttempts: 10)

        XCTAssertEqual(sleeper.delays.suffix(2).map { $0 }, [30, 30])
    }

    func testReconnectResetsBackoffAfterSuccessfulStatus() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()  // fetchStatus succeeds throughout
        let events = FakeEventsSubscribing()
        events.autoFinishCount = 100
        let sleeper = FakeSleeper()
        let coord = SyncCoordinator(model: model, admin: admin, events: events, sleeper: sleeper)

        try await coord.bootstrap()
        try await coord.runStreamWithReconnect(maxAttempts: 5)

        // Each attempt: drop → sleep(1s) → fetchStatus succeeds → reset → next loop.
        // So every sleep stays at 1s (not exponential growth) since RPC keeps working.
        XCTAssertEqual(sleeper.delays, [1, 1, 1, 1, 1])
    }

    func testReconnectMarksDaemonDownAfterThreeStatusFailures() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        let events = FakeEventsSubscribing()
        let sleeper = FakeSleeper()
        let coord = SyncCoordinator(model: model, admin: admin, events: events, sleeper: sleeper)

        // bootstrap fails (daemon down) → status starts as .down
        admin.shouldThrow = AdminClientError.connectFailed
        events.subscribeError = AdminClientError.connectFailed
        try await coord.bootstrap()
        // Force a transition to .up so the reconnect path observes 3 status failures
        // demoting back to .down (proves the failure counter triggers the demotion).
        model.daemonStatus = .up(stats: .zero, uptime: 0, paused: false)

        try await coord.runStreamWithReconnect(maxAttempts: 4)

        XCTAssertEqual(model.daemonStatus, .down(reason: .notRunning))
    }
```

Note : la `FakeEventsSubscribing` créée Task 7 supporte déjà `autoFinishCount` (cf code complet plus haut). Les tests ci-dessus s'appuient sur ce champ pour forcer plusieurs cycles subscribe → finish sans gérer de Task en background. Pas de modification de la fake nécessaire ici.

- [ ] **Step 4: Implémenter `runStreamWithReconnect`**

Modifier l'init du coordinator pour accepter le sleeper :

```swift
private let sleeper: AsyncSleeper

public init(
    model: AppModel,
    admin: AdminCalling,
    events: EventsSubscribing,
    sleeper: AsyncSleeper = SystemSleeper(),
    logger: Logger = Logger(label: "io.iris.app.sync")
) {
    self.model = model
    self.admin = admin
    self.events = events
    self.sleeper = sleeper
    self.logger = logger
}
```

Ajouter à la fin de la classe :

```swift
    /// Reconnect loop. `maxAttempts == nil` runs forever (production). Tests pass a finite cap.
    public func runStreamWithReconnect(maxAttempts: Int? = nil) async throws {
        let backoffs: [Double] = [1, 2, 4, 8, 16, 30]
        var statusFailures = 0
        var attempt = 0
        while true {
            if let max = maxAttempts, attempt >= max { return }
            attempt += 1
            do {
                try await runStream()
                // Stream finished cleanly (server closed) → treat as transient drop.
            } catch is CancellationError {
                return
            } catch {
                logger.warning("SSE stream error: \(error)")
            }

            let delay = backoffs[min(attempt - 1, backoffs.count - 1)]
            try await sleeper.sleep(seconds: delay)

            do {
                let status = try await admin.fetchStatus()
                statusFailures = 0
                attempt = 0  // Reset backoff so a future drop after stability starts at 1s.
                if case .up(_, _, let paused) = model.daemonStatus {
                    model.daemonStatus = .up(
                        stats: status.stats,
                        uptime: TimeInterval(status.uptimeS),
                        paused: paused
                    )
                } else {
                    model.daemonStatus = .up(
                        stats: status.stats,
                        uptime: TimeInterval(status.uptimeS),
                        paused: false
                    )
                }
            } catch {
                statusFailures += 1
                if statusFailures >= 3 {
                    model.daemonStatus = .down(reason: .notRunning)
                }
            }
        }
    }
```

- [ ] **Step 5: Run tests, verify pass**

Run: `swift test --filter SyncCoordinatorTests`
Expected: 9 tests pass.

- [ ] **Step 6: Commit**

```bash
git add Sources/IrisAppCore Tests/IrisAppCoreTests
git commit -m "feat(phase-6.1a): SyncCoordinator reconnect with exponential backoff 1->30s"
```

---

## Task 10 : `SyncCoordinator` periodic `daemon.stats` poll (5 s)

**Files:**
- Modify: `Sources/IrisAppCore/SyncCoordinator.swift`
- Modify: `Tests/IrisAppCoreTests/SyncCoordinatorTests.swift`

Méthode `runStatsPoll(intervalSeconds:maxTicks:)` qui boucle : sleep N → `fetchStats` → met à jour `daemonStatus.stats` si daemon up. Ignore les erreurs (logger warn), n'altère pas `daemonStatus` (la SSE loop gère l'état down).

- [ ] **Step 1: Ajouter le test**

```swift
    func testStatsPollUpdatesDaemonStatsWhenUp() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        let events = FakeEventsSubscribing()
        let sleeper = FakeSleeper()
        let coord = SyncCoordinator(model: model, admin: admin, events: events, sleeper: sleeper)

        model.daemonStatus = .up(stats: .zero, uptime: 0, paused: false)
        admin.stubStats = DaemonStats(reqTotal: 42, subTotal: 7, exfilBlockedTotal: 1, errorsTotal: 0)

        try await coord.runStatsPoll(intervalSeconds: 5, maxTicks: 3)

        XCTAssertEqual(sleeper.delays, [5, 5, 5])
        if case .up(let stats, _, _) = model.daemonStatus {
            XCTAssertEqual(stats.reqTotal, 42)
        } else {
            XCTFail("expected up")
        }
    }

    func testStatsPollSkipsUpdateIfNotUp() async throws {
        let model = AppModel(defaults: UserDefaults(suiteName: UUID().uuidString)!)
        let admin = FakeAdminCalling()
        let events = FakeEventsSubscribing()
        let sleeper = FakeSleeper()
        let coord = SyncCoordinator(model: model, admin: admin, events: events, sleeper: sleeper)

        model.daemonStatus = .down(reason: .notRunning)

        try await coord.runStatsPoll(intervalSeconds: 5, maxTicks: 2)

        XCTAssertEqual(model.daemonStatus, .down(reason: .notRunning))
        XCTAssertEqual(admin.calls.filter { $0 == "stats" }.count, 0)
    }
```

- [ ] **Step 2: Implémenter `runStatsPoll`**

```swift
    public func runStatsPoll(intervalSeconds: Double = 5, maxTicks: Int? = nil) async throws {
        var ticks = 0
        while true {
            if let max = maxTicks, ticks >= max { return }
            ticks += 1
            try await sleeper.sleep(seconds: intervalSeconds)
            guard case .up(_, let uptime, let paused) = model.daemonStatus else { continue }
            do {
                let stats = try await admin.fetchStats()
                model.daemonStatus = .up(stats: stats, uptime: uptime, paused: paused)
            } catch {
                logger.debug("stats poll skipped: \(error)")
            }
        }
    }
```

- [ ] **Step 3: Run tests, verify pass**

Run: `swift test --filter SyncCoordinatorTests`
Expected: 10 tests pass.

- [ ] **Step 4: Commit**

```bash
git add Sources/IrisAppCore Tests/IrisAppCoreTests/SyncCoordinatorTests.swift
git commit -m "feat(phase-6.1a): SyncCoordinator periodic daemon.stats poll"
```

---

## Task 11 : `NotificationBuilder` + invariant no-secret-leak

**Files:**
- Create: `Sources/IrisAppCore/NotificationBuilder.swift`
- Create: `Tests/IrisAppCoreTests/NotificationBuilderTests.swift`

Pure function `Event → UNMutableNotificationContent?`. Retourne nil si pas une alerte de severity ≥ medium. UserInfo porte l'event UUID.

`UserNotifications` est disponible côté library SwiftPM. Pas de conformance Sendable requise puisqu'on la passe au main thread.

- [ ] **Step 1: Écrire les tests**

`Tests/IrisAppCoreTests/NotificationBuilderTests.swift` :

```swift
import XCTest
import UserNotifications
import IrisKit
@testable import IrisAppCore

final class NotificationBuilderTests: XCTestCase {
    private func makeAlertEvent(
        severity: Alert.Severity,
        kind: Event.Kind = .exfilBlocked,
        secretName: String = "anthropic_api_key",
        host: String = "api.github.com",
        snippet: String = "x-api-key: REDACTED"
    ) -> Event {
        let alert = Alert(
            severity: severity, rule: .hostMismatch,
            secretName: secretName, detectedAt: .header, snippet: snippet
        )
        return Event(
            timestamp: Date(), kind: kind, host: host,
            method: "POST", path: "/issues",
            alert: kind == .exfilBlocked ? alert : nil
        )
    }

    func testReturnsNilForSubstitutedEvent() {
        let evt = Event(
            timestamp: Date(), kind: .substituted, host: "x.com",
            method: "GET", path: "/"
        )
        XCTAssertNil(NotificationBuilder.build(from: evt))
    }

    func testReturnsNilForLowSeverityAlert() {
        let evt = makeAlertEvent(severity: .low)
        XCTAssertNil(NotificationBuilder.build(from: evt))
    }

    func testReturnsContentForMediumSeverity() {
        let evt = makeAlertEvent(severity: .medium)
        let content = NotificationBuilder.build(from: evt)
        XCTAssertNotNil(content)
        XCTAssertEqual(content?.title, "Exfiltration attempt blocked")
        XCTAssertTrue(content?.subtitle.contains("anthropic_api_key") == true)
        XCTAssertTrue(content?.subtitle.contains("api.github.com") == true)
    }

    func testUserInfoCarriesEventID() {
        let evt = makeAlertEvent(severity: .high)
        let content = NotificationBuilder.build(from: evt)
        XCTAssertEqual(content?.userInfo["event_id"] as? String, evt.id.uuidString)
    }

    /// CLAUDE.md §6.2 invariant: notification content NEVER leaks the raw secret value.
    /// We construct an event whose snippet (Alert.snippet) is by design redacted, then assert
    /// that the rendered title/subtitle/body never substring-contains a known secret value
    /// that we DID NOT pass into the alert. This locks the contract: builder never invents
    /// or splices values from anywhere other than alert.snippet (which is redaction-locked
    /// by Phase 4 PlaceholderScanner).
    func testNotificationContentNeverContainsSecretValue() {
        let dangerousValue = "sk-ant-this-must-never-appear-12345"
        let evt = makeAlertEvent(
            severity: .high,
            secretName: "anthropic_api_key",
            snippet: "x-api-key: {{kc:anthropic_api_key}}"
        )
        let content = NotificationBuilder.build(from: evt)!
        XCTAssertFalse(content.title.contains(dangerousValue))
        XCTAssertFalse(content.subtitle.contains(dangerousValue))
        XCTAssertFalse(content.body.contains(dangerousValue))
    }
}
```

- [ ] **Step 2: Implémenter `NotificationBuilder`**

`Sources/IrisAppCore/NotificationBuilder.swift` :

```swift
import Foundation
import UserNotifications
import IrisKit

public enum NotificationBuilder {
    /// SPECS §15.3 — notify only on exfiltration alerts of severity >= medium.
    public static func build(from event: Event) -> UNMutableNotificationContent? {
        guard event.kind == .exfilBlocked, let alert = event.alert else { return nil }
        guard alert.severity >= .medium else { return nil }

        let content = UNMutableNotificationContent()
        content.title = "Exfiltration attempt blocked"
        content.subtitle = "\(alert.secretName) → \(event.host)"
        content.body = "Rule: \(ruleLabel(alert.rule)). Click to inspect."
        content.userInfo = ["event_id": event.id.uuidString]
        return content
    }

    private static func ruleLabel(_ rule: Alert.ExfilRule) -> String {
        switch rule {
        case .hostMismatch: return "host mismatch"
        case .nonCanonicalLocation: return "non-canonical location"
        case .multipleSecrets: return "multiple secrets in one request"
        case .suspiciousContentType: return "suspicious content type"
        case .volumeAnomaly: return "volume anomaly"
        }
    }
}
```

- [ ] **Step 3: Run tests, verify pass**

Run: `swift test --filter NotificationBuilderTests`
Expected: 5 tests pass.

- [ ] **Step 4: Commit**

```bash
git add Sources/IrisAppCore/NotificationBuilder.swift Tests/IrisAppCoreTests/NotificationBuilderTests.swift
git commit -m "feat(phase-6.1a): NotificationBuilder with no-secret-leak invariant test"
```

---

## Task 12 : `swift-format lint --strict` + full `swift test` baseline

**Files:** none

Vérifier que toute la branche passe le linter strict et que la suite complète de tests est verte.

- [ ] **Step 1: Lint strict**

Run: `swift-format lint --strict --recursive Sources Tests`
Expected: exit code 0, no output.

Si des erreurs apparaissent, corriger inline (les corrections sont mineures : import ordering, indentation, line length). Re-commiter avec `style: fix swift-format violations` si nécessaire.

- [ ] **Step 2: Full test suite**

Run: `swift test`
Expected: tous les tests passent. Compter le delta : baseline `329` → attendu `329 + ~26 (Task 2: 5 + Task 4: 3 + Task 5: 6 + Task 6: 3 + Task 7: 2 + Task 8: 3 + Task 9: 4 + Task 10: 2 + Task 11: 5) = 362 environ`.

- [ ] **Step 3: Release build**

Run: `swift build -c release`
Expected: build clean, 0 warnings.

Si warnings dans IrisAppCore : fixer en suivant les patterns Phase 2.2 (NIOLoopBound seulement si touche NIO, sinon `@MainActor` / `Sendable` annotations). Ces fixes vont dans un commit `chore: fix strict-concurrency warnings`.

---

## Task 13 : Push branch + open PR 6.1a

**Files:** none (PR sur GitHub)

- [ ] **Step 1: Push branch**

```bash
git push -u origin feat/phase-6.1a-irisappcore
```

- [ ] **Step 2: Open PR with smoke checklist**

```bash
gh pr create --title "feat(phase-6.1a): IrisAppCore SwiftPM library (AppModel + SyncCoordinator + NotificationBuilder)" --body "$(cat <<'EOF'
## Summary

Phase 6.1a : library SwiftPM `IrisAppCore` qui contient toute la logique non-UI du menu-bar :

- `AppModel` (`@MainActor ObservableObject`) — state model avec event ring (cap 1000), alerts derivation, acknowledgment persistance UserDefaults, pause/resume action.
- `SyncCoordinator` (`@MainActor`) — bootstrap (snapshot RPC) + SSE consumption (`since:` continuation) + reconnect exponentiel 1→30s + periodic `daemon.stats` poll 5s, avec `AsyncSleeper` injectable.
- `NotificationBuilder` (pure) — `Event → UNMutableNotificationContent?` avec invariant no-secret-leak verrouillé.
- Protocoles `AdminCalling` + `EventsSubscribing` pour mockabilité, conformance des types IrisKit concrets.
- Modèles UI : `DaemonStatus` (state machine), `LogFilters` (predicate logique).

Aucune nouvelle dépendance. Aucune modification IrisKit / irisd / iris. PR 6.1b ajoutera l'Xcode project + UI qui consomme cette library.

## Spec source

`docs/superpowers/specs/2026-05-28-phase-6.1-menubar-readonly-design.md`

## Smoke testing

- [ ] `swift build -c release` clean, 0 warnings.
- [ ] `swift test` passe (delta +25 tests).
- [ ] `swift-format lint --strict --recursive Sources Tests` clean.
- [ ] `IrisAppCoreTests/NotificationBuilderTests.testNotificationContentNeverContainsSecretValue` est présent et passe.
EOF
)"
```

- [ ] **Step 3: Trigger Gemini review polling**

Lancer le polling Gemini per CLAUDE.md §8 : `/loop 30s` avec prompt auto-arrêtant après 10 min de silence (référence pattern `docs/superpowers/plans/2026-05-26-phase-5.3.1-jsonc-watch.md` Task 6).

---

# PR 6.1b — IrisApp Xcode project + UI

> **Pré-requis** : PR 6.1a mergée sur main. Repartir d'une `main` à jour.

## Task 14 : Bootstrap Xcode project (étape manuelle utilisateur)

**Files:**
- Create: `IrisApp/IrisApp.xcodeproj/` (entier, via Xcode UI)
- Create: `IrisApp/IrisApp.swift`
- Create: `IrisApp/Assets.xcassets/`

L'agent **ne peut pas créer un `.xcodeproj` valide depuis la CLI**. Cette étape doit être exécutée par l'utilisateur dans Xcode UI, une fois pour toutes. L'agent reprend après le commit du squelette.

- [ ] **Step 1: Préparer la branche**

```bash
git checkout main && git pull
git checkout -b feat/phase-6.1b-menubar-ui
mkdir -p IrisApp
```

- [ ] **Step 2: Instructions utilisateur (à exécuter dans Xcode UI)**

1. Ouvrir Xcode → File → New → Project…
2. macOS → App → Next
3. Product Name: `Iris`
4. Team: ton équipe (personal team OK pour dev)
5. Organization Identifier: `io.iris` (le bundle ID devient `io.iris.Iris` automatiquement)
6. Interface: SwiftUI
7. Language: Swift
8. Storage: None
9. Decoché "Include Tests" (on n'a pas de tests UI dans cette PR)
10. Next → naviguer vers `<repo>/IrisApp/` → Create
11. Dans le navigator, ouvrir `Iris.xcodeproj` → cliquer sur le projet → target Iris → General :
    - Bundle Identifier : `io.iris.app` (écraser)
    - Display Name : `Iris`
    - Minimum Deployments : macOS 13.0
    - App Category : Developer Tools
12. Target Iris → Signing & Capabilities : Signing Certificate = "Sign to Run Locally" (ad-hoc).
13. File → Add Package Dependencies… → Add Local… → naviguer vers le repo root (`<repo>/`) → Add Package. Sélectionner `IrisAppCore` + `IrisKit` à ajouter au target Iris.
14. Fermer Xcode (sauvegarde auto).

- [ ] **Step 3: Vérifier le contenu créé**

```bash
ls IrisApp/
# Attendu: Iris/  Iris.xcodeproj/
ls IrisApp/Iris/
# Attendu: Assets.xcassets/  ContentView.swift  IrisApp.swift  Iris.entitlements  Preview\ Content/
```

Renommer le sous-dossier pour cohérence avec le spec layout :

```bash
mv IrisApp/Iris/IrisApp.swift IrisApp/Iris/AppEntry.swift
# Note: on garde ContentView.swift comme placeholder, on l'écrasera Task 18 par PopoverView
```

- [ ] **Step 4: Stage et commit le scaffold**

```bash
git add IrisApp
git status  # vérifier qu'aucun fichier Xcode user-specific n'est inclus
git commit -m "chore(phase-6.1b): bootstrap IrisApp.xcodeproj via Xcode (LSUIElement to follow)"
```

---

## Task 15 : `.gitignore` Xcode user-specific files + Info.plist keys

**Files:**
- Modify: `.gitignore`
- Modify: `IrisApp/Iris/Info.plist` (ou créé si absent)
- Modify: `IrisApp/Iris.xcodeproj/project.pbxproj` (via Xcode UI ou manuel)

- [ ] **Step 1: Ajouter Xcode patterns à `.gitignore`**

Ajouter à la fin de `.gitignore` :

```
# Xcode
**/xcuserdata/
**/*.xcuserstate
**/DerivedData/
```

- [ ] **Step 2: Créer/éditer Info.plist**

Si `IrisApp/Iris/Info.plist` n'existe pas (Xcode 15+ gère les keys dans pbxproj par défaut), il faut configurer via l'éditeur Xcode :

- Ouvrir Xcode → projet Iris → target Iris → Info tab
- Ajouter (ou modifier) :
  - `Application is agent (UIElement)` (`LSUIElement`) = `YES`
  - `App Transport Security Settings` → `Allow Local Networking` = `YES`
  - `Minimum macOS Version` = `13.0` (déjà set Task 14)

Sauvegarder, fermer Xcode.

- [ ] **Step 3: Vérifier dans pbxproj que les keys sont persistées**

```bash
grep -E "LSUIElement|NSAppTransportSecurity|NSAllowsLocalNetworking" IrisApp/Iris.xcodeproj/project.pbxproj
```

Expected : au moins 1 match pour chacune. Si rien : retourner dans Xcode et ré-éditer manuellement le .plist auto-généré.

- [ ] **Step 4: Vérifier build**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -20
```

Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 5: Commit**

```bash
git add .gitignore IrisApp
git commit -m "chore(phase-6.1b): LSUIElement=YES + NSAllowsLocalNetworking + xcuserdata gitignore"
```

---

## Task 16 : `AppDelegate` + `NSStatusItem` + popover wiring

**Files:**
- Modify: `IrisApp/Iris/AppEntry.swift`
- Create: `IrisApp/Iris/AppDelegate.swift`
- Modify: `IrisApp/Iris/ContentView.swift` (devient placeholder PopoverView pour cette task)

Squelette minimal : status item avec icône template, popover qui s'affiche au click, contenu = un Text placeholder. Pas encore d'AppModel injection (Task 18).

- [ ] **Step 1: Réécrire `AppEntry.swift`**

`IrisApp/Iris/AppEntry.swift` :

```swift
import SwiftUI

@main
struct IrisApp: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) var appDelegate

    var body: some Scene {
        Settings {
            EmptyView()
        }
    }
}
```

- [ ] **Step 2: Créer `AppDelegate.swift`**

`IrisApp/Iris/AppDelegate.swift` :

```swift
import AppKit
import SwiftUI

final class AppDelegate: NSObject, NSApplicationDelegate {
    private var statusItem: NSStatusItem!
    private var popover: NSPopover!

    func applicationDidFinishLaunching(_ notification: Notification) {
        // Multi-instance protection (SPECS §3.3 + §4 edge cases).
        let same = NSRunningApplication.runningApplications(
            withBundleIdentifier: Bundle.main.bundleIdentifier ?? "io.iris.app"
        )
        if same.count > 1 {
            NSApp.terminate(nil)
            return
        }

        statusItem = NSStatusBar.system.statusItem(withLength: NSStatusItem.variableLength)
        if let button = statusItem.button {
            // Template icon: monochrome SF Symbol. Replaced by xcassets in Task 19.
            button.image = NSImage(
                systemSymbolName: "key.fill",
                accessibilityDescription: "Iris"
            )
            button.image?.isTemplate = true
            button.target = self
            button.action = #selector(handleClick(_:))
            button.sendAction(on: [.leftMouseUp, .rightMouseUp])
        }

        popover = NSPopover()
        popover.behavior = .transient
        popover.contentSize = NSSize(width: 480, height: 600)
        popover.contentViewController = NSHostingController(
            rootView: PopoverView()
        )
    }

    @objc private func handleClick(_ sender: NSStatusBarButton) {
        guard let event = NSApp.currentEvent else { togglePopover(); return }
        if event.type == .rightMouseUp {
            showQuitMenu(from: sender)
        } else {
            togglePopover()
        }
    }

    private func togglePopover() {
        guard let button = statusItem.button else { return }
        if popover.isShown {
            popover.performClose(nil)
        } else {
            popover.show(relativeTo: button.bounds, of: button, preferredEdge: .minY)
            popover.contentViewController?.view.window?.makeKey()
        }
    }

    private func showQuitMenu(from button: NSStatusBarButton) {
        let menu = NSMenu()
        menu.addItem(
            NSMenuItem(title: "Quit Iris", action: #selector(quit), keyEquivalent: "q")
        )
        menu.items.forEach { $0.target = self }
        // NSMenu.popUp(positioning:at:in:) is the modern (non-deprecated) replacement for
        // statusItem.popUpMenu(_:). Anchored to the button so the menu drops down from the
        // status item. Avoids the recursion risk of synthesising a click via performClick
        // (which would re-enter handleClick and could re-trigger showQuitMenu).
        menu.popUp(
            positioning: nil,
            at: NSPoint(x: 0, y: button.bounds.height),
            in: button
        )
    }

    @objc private func quit() {
        NSApp.terminate(nil)
    }
}
```

- [ ] **Step 3: Réécrire `ContentView.swift` en `PopoverView.swift`**

Renommer/réécrire `IrisApp/Iris/ContentView.swift` en `IrisApp/Iris/PopoverView.swift` :

```swift
import SwiftUI

struct PopoverView: View {
    var body: some View {
        VStack {
            Text("Iris menu bar — Phase 6.1b WIP")
                .padding()
            Text("Real content lands in Task 18.")
                .font(.caption)
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
```

Dans Xcode : le fichier ContentView.swift référencé dans pbxproj doit être renommé. Ouvrir Xcode → clic droit sur ContentView.swift → "Rename…" → `PopoverView.swift`. Vérifier que `IrisApp.swift` (ancien nom auto-généré, déjà renommé `AppEntry.swift` en Task 14 Step 3) ne référence plus `ContentView`.

- [ ] **Step 4: Build + smoke**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -10
```

Expected: `** BUILD SUCCEEDED **`.

Pour test runtime : lancer depuis Xcode (Run button). L'icône clé apparaît dans la status bar. Left-click → popover s'affiche. Right-click → menu "Quit Iris" → app quitte.

- [ ] **Step 5: Commit**

```bash
git add IrisApp/Iris
git commit -m "feat(phase-6.1b): NSStatusItem + NSPopover skeleton with placeholder content"
```

---

## Task 17 : `NotificationCoordinator` + permission request

**Files:**
- Create: `IrisApp/Iris/NotificationCoordinator.swift`
- Modify: `IrisApp/Iris/AppDelegate.swift`

Délégué `UNUserNotificationCenterDelegate`. Demande permission au launch. Au click sur notif : set `focusedAlertID` + `selectedTab = .security` + ouvre popover.

Cette task introduit l'injection d'`AppModel` (qui sera complète Task 18).

- [ ] **Step 1: Créer `NotificationCoordinator.swift`**

```swift
import AppKit
import IrisAppCore
import UserNotifications

@MainActor
final class NotificationCoordinator: NSObject, UNUserNotificationCenterDelegate {
    private weak var model: AppModel?
    private let onOpenPopover: @MainActor () -> Void

    init(model: AppModel, onOpenPopover: @escaping @MainActor () -> Void) {
        self.model = model
        self.onOpenPopover = onOpenPopover
        super.init()
        UNUserNotificationCenter.current().delegate = self
    }

    func requestAuthorization() async {
        let center = UNUserNotificationCenter.current()
        do {
            let granted = try await center.requestAuthorization(options: [.alert, .sound])
            await MainActor.run { self.model?.notificationsEnabled = granted }
        } catch {
            await MainActor.run { self.model?.notificationsEnabled = false }
        }
    }

    func emit(_ content: UNMutableNotificationContent) {
        let request = UNNotificationRequest(
            identifier: UUID().uuidString,
            content: content,
            trigger: nil
        )
        UNUserNotificationCenter.current().add(request, withCompletionHandler: nil)
    }

    // MARK: - UNUserNotificationCenterDelegate

    nonisolated func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        willPresent notification: UNNotification,
        withCompletionHandler completionHandler:
            @escaping @Sendable (UNNotificationPresentationOptions) -> Void
    ) {
        completionHandler([.banner, .sound])
    }

    nonisolated func userNotificationCenter(
        _ center: UNUserNotificationCenter,
        didReceive response: UNNotificationResponse,
        withCompletionHandler completionHandler: @escaping @Sendable () -> Void
    ) {
        let uid = response.notification.request.content.userInfo["event_id"] as? String
        let eventID = uid.flatMap(UUID.init(uuidString:))
        Task { @MainActor in
            self.model?.selectedTab = .security
            if let id = eventID { self.model?.focusedAlertID = id }
            self.onOpenPopover()
            completionHandler()
        }
    }
}
```

- [ ] **Step 2: Brancher dans `AppDelegate`**

Ajouter en haut de `AppDelegate` :

```swift
import IrisAppCore
import IrisKit
import UserNotifications

private let appModel = AppModel()
private var notifications: NotificationCoordinator!
```

Dans `applicationDidFinishLaunching`, après le multi-instance check et avant le `statusItem` setup :

```swift
        notifications = NotificationCoordinator(model: appModel) { [weak self] in
            self?.openPopover()
        }
        Task { await notifications.requestAuthorization() }
```

Ajouter méthode `openPopover()` extraite de `togglePopover` :

```swift
    func openPopover() {
        guard let button = statusItem.button, !popover.isShown else { return }
        popover.show(relativeTo: button.bounds, of: button, preferredEdge: .minY)
        popover.contentViewController?.view.window?.makeKey()
    }
```

- [ ] **Step 3: Build smoke**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -10
```

Expected: build success.

Runtime smoke : lancer depuis Xcode. La première fois → macOS demande permission notification. Accepter. (Renvoie nil event_id si testé sans event réel — c'est fine, juste vérifier que la popup macOS apparaît.)

- [ ] **Step 4: Commit**

```bash
git add IrisApp/Iris
git commit -m "feat(phase-6.1b): NotificationCoordinator with permission request + click routing"
```

---

## Task 18 : Wire AppModel + SyncCoordinator dans AppDelegate, écrire vraie `PopoverView`

**Files:**
- Modify: `IrisApp/Iris/AppDelegate.swift`
- Modify: `IrisApp/Iris/PopoverView.swift`

Construire `SyncCoordinator` au launch avec `AdminClient` réel + `EventsClient` réel sur les paths/ports default. Lancer les 3 tasks (bootstrap → stream + stats poll). Inject `appModel` dans `PopoverView` via `.environmentObject`.

Default paths (lus depuis `Config.default` ou hardcodés en Phase 6.1, le settings tab les rendra configurables en Phase 6.3) :

- admin socket : `~/Library/Application Support/iris/admin.sock`
- events port : `8899` (cf `BrokerConfig.default`)

- [ ] **Step 1: Modifier `AppDelegate.applicationDidFinishLaunching`**

Remplacer la section après `notifications = ...` par :

```swift
        let admin = AdminClient(socketPath: defaultAdminSocketPath())
        let events = EventsClient(port: 8899)
        let coord = SyncCoordinator(model: appModel, admin: admin, events: events)

        Task {
            try? await coord.bootstrap()
            await withTaskGroup(of: Void.self) { group in
                group.addTask { try? await coord.runStreamWithReconnect() }
                group.addTask { try? await coord.runStatsPoll() }
                // Notification fan-out: observe new alerts and emit notifications.
                group.addTask { @MainActor in
                    var seenIDs: Set<UUID> = []
                    for await alerts in appModel.$alerts.values {
                        for alert in alerts where !seenIDs.contains(alert.id) {
                            seenIDs.insert(alert.id)
                            if let content = NotificationBuilder.build(from: alert) {
                                notifications.emit(content)
                            }
                        }
                    }
                }
            }
        }
```

Ajouter le helper en haut du fichier :

```swift
private func defaultAdminSocketPath() -> String {
    let raw = "~/Library/Application Support/iris/admin.sock"
    return (raw as NSString).expandingTildeInPath
}
```

Et l'import :

```swift
import Combine
```

Modifier `popover.contentViewController = NSHostingController(rootView: PopoverView())` pour :

```swift
        popover.contentViewController = NSHostingController(
            rootView: PopoverView().environmentObject(appModel)
        )
```

- [ ] **Step 2: Réécrire `PopoverView` (header + segmented control + router)**

`IrisApp/Iris/PopoverView.swift` :

```swift
import IrisAppCore
import IrisKit
import SwiftUI

struct PopoverView: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        VStack(spacing: 0) {
            HeaderBar()
                .padding(.horizontal, 12)
                .padding(.vertical, 8)
                .background(.bar)
            Divider()
            Picker("Tab", selection: $model.selectedTab) {
                Text("Overview").tag(AppModel.Tab.overview)
                Text("Logs").tag(AppModel.Tab.logs)
                Text("Security").tag(AppModel.Tab.security)
            }
            .pickerStyle(.segmented)
            .padding(.horizontal, 12)
            .padding(.vertical, 6)
            Divider()
            Group {
                switch model.selectedTab {
                case .overview: OverviewTab()
                case .logs: LogsTab()
                case .security: SecurityTab()
                }
            }
        }
        .frame(width: 480, height: 600)
    }
}

private struct HeaderBar: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        HStack(spacing: 10) {
            StatusDot(status: model.daemonStatus)
            StatusLabel(status: model.daemonStatus)
            Spacer()
            if case .up(_, _, let paused) = model.daemonStatus {
                Button(paused ? "Resume" : "Pause") {
                    // togglePause is async; fire-and-forget for UI responsiveness.
                    // The AdminClient is recreated here for the call (cheap: just a socket path).
                    Task { try? await model.togglePause(via: makeAdmin()) }
                }
                .buttonStyle(.bordered)
            }
        }
    }

    private func makeAdmin() -> AdminCalling {
        AdminClient(socketPath: defaultAdminSocketPath())
    }
}

private struct StatusDot: View {
    let status: DaemonStatus
    var body: some View {
        Circle().fill(color).frame(width: 8, height: 8)
    }
    private var color: Color {
        switch status {
        case .up(_, _, let paused): return paused ? .orange : .green
        case .down: return .red
        case .connecting: return .gray
        }
    }
}

private struct StatusLabel: View {
    let status: DaemonStatus
    var body: some View {
        switch status {
        case .up(_, let uptime, let paused):
            Text("\(paused ? "Paused" : "Up") • \(formatUptime(uptime))")
                .font(.callout.monospacedDigit())
        case .down: Text("Daemon down").foregroundStyle(.red).font(.callout)
        case .connecting: Text("Connecting…").font(.callout)
        }
    }
    private func formatUptime(_ seconds: TimeInterval) -> String {
        let s = Int(seconds)
        let h = s / 3600
        let m = (s % 3600) / 60
        if h > 0 { return "\(h)h \(m)m" }
        return "\(m)m"
    }
}
```

Helper top-level dans le même fichier :

```swift
fileprivate func defaultAdminSocketPath() -> String {
    let raw = "~/Library/Application Support/iris/admin.sock"
    return (raw as NSString).expandingTildeInPath
}
```

- [ ] **Step 3: Build smoke (les 3 onglets sont des `EmptyView` jusqu'aux Tasks 19-21)**

Créer 3 fichiers stubs pour que `PopoverView` compile :

`IrisApp/Iris/OverviewTab.swift`, `IrisApp/Iris/LogsTab.swift`, `IrisApp/Iris/SecurityTab.swift` :

```swift
import SwiftUI

struct OverviewTab: View { var body: some View { Text("Overview — TODO") } }
struct LogsTab: View { var body: some View { Text("Logs — TODO") } }
struct SecurityTab: View { var body: some View { Text("Security — TODO") } }
```

(Ces stubs sont des placeholders explicites — chacun sera remplacé dans sa propre task. Les TODO sont temporaires.)

Build :

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -10
```

Expected: build success.

- [ ] **Step 4: Commit**

```bash
git add IrisApp/Iris
git commit -m "feat(phase-6.1b): wire SyncCoordinator + PopoverView header/segmented control"
```

---

## Task 19 : `OverviewTab` (24h counters + last 5 events)

**Files:**
- Modify: `IrisApp/Iris/OverviewTab.swift`

- [ ] **Step 1: Implémenter**

```swift
import IrisAppCore
import IrisKit
import SwiftUI

struct OverviewTab: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 16) {
                countersSection
                Divider()
                recentSection
            }
            .padding(12)
        }
    }

    @ViewBuilder private var countersSection: some View {
        let stats = currentStats()
        VStack(alignment: .leading, spacing: 4) {
            Text("Since daemon start").font(.headline)
            HStack(spacing: 24) {
                counter(label: "Requests", value: stats.reqTotal)
                counter(label: "Substituted", value: stats.subTotal)
                counter(label: "Blocked", value: stats.exfilBlockedTotal)
                counter(label: "Errors", value: stats.errorsTotal)
            }
        }
    }

    private func currentStats() -> DaemonStats {
        if case .up(let stats, _, _) = model.daemonStatus { return stats }
        return .zero
    }

    private func counter(label: String, value: UInt64) -> some View {
        VStack(alignment: .leading) {
            Text("\(value)").font(.title2.bold().monospacedDigit())
            Text(label).font(.caption).foregroundStyle(.secondary)
        }
    }

    @ViewBuilder private var recentSection: some View {
        VStack(alignment: .leading, spacing: 4) {
            Text("Recent events").font(.headline)
            if model.events.isEmpty {
                Text("No events yet.").foregroundStyle(.secondary).font(.callout)
            } else {
                ForEach(Array(model.events.prefix(5))) { event in
                    EventRow(event: event)
                }
            }
        }
    }
}

struct EventRow: View {
    let event: Event
    var body: some View {
        HStack(spacing: 8) {
            Text(timeString(event.timestamp)).font(.caption.monospacedDigit())
                .foregroundStyle(.secondary)
            kindBadge
            Text(event.host).font(.callout)
            Text(event.path).font(.callout).foregroundStyle(.secondary).lineLimit(1)
            Spacer()
        }
    }
    private var kindBadge: some View {
        Text(event.kind.rawValue).font(.caption.bold())
            .padding(.horizontal, 6).padding(.vertical, 2)
            .background(color.opacity(0.2)).foregroundStyle(color)
            .clipShape(Capsule())
    }
    private var color: Color {
        switch event.kind {
        case .substituted: return .green
        case .passThrough: return .gray
        case .noMatch: return .gray
        case .exfilBlocked: return .red
        case .error: return .orange
        }
    }
    private func timeString(_ date: Date) -> String {
        let f = DateFormatter()
        f.dateFormat = "HH:mm:ss"
        return f.string(from: date)
    }
}
```

- [ ] **Step 2: Build smoke + commit**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -5
git add IrisApp/Iris/OverviewTab.swift
git commit -m "feat(phase-6.1b): OverviewTab with counters + last 5 events"
```

---

## Task 20 : `LogsTab` (live stream + filtres + pause toggle)

**Files:**
- Modify: `IrisApp/Iris/LogsTab.swift`

- [ ] **Step 1: Implémenter**

```swift
import IrisAppCore
import IrisKit
import SwiftUI

struct LogsTab: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        VStack(spacing: 0) {
            filterBar
            Divider()
            list
        }
    }

    private var filteredEvents: [Event] {
        let base = model.streamPaused ? snapshotEvents : model.events
        return base.filter { model.logFilters.matches($0) }
    }

    @State private var snapshotEvents: [Event] = []

    private var filterBar: some View {
        HStack(spacing: 8) {
            TextField("Search", text: $model.logFilters.search).textFieldStyle(.roundedBorder)
            TextField("Host", text: $model.logFilters.host).textFieldStyle(.roundedBorder)
                .frame(width: 140)
            Menu {
                ForEach(Event.Kind.allCases, id: \.self) { kind in
                    Button {
                        toggleKind(kind)
                    } label: {
                        Label(
                            kind.rawValue,
                            systemImage: model.logFilters.kinds.contains(kind) ? "checkmark" : ""
                        )
                    }
                }
            } label: { Label("Kinds", systemImage: "line.3.horizontal.decrease.circle") }
                .menuStyle(.borderlessButton).frame(width: 100)
            Toggle("Pause", isOn: pauseBinding).toggleStyle(.button)
        }
        .padding(.horizontal, 12)
        .padding(.vertical, 6)
    }

    private var pauseBinding: Binding<Bool> {
        Binding(
            get: { model.streamPaused },
            set: { newValue in
                if newValue { snapshotEvents = model.events }
                model.streamPaused = newValue
            }
        )
    }

    private func toggleKind(_ kind: Event.Kind) {
        if model.logFilters.kinds.contains(kind) {
            model.logFilters.kinds.remove(kind)
        } else {
            model.logFilters.kinds.insert(kind)
        }
    }

    private var list: some View {
        List(filteredEvents) { event in
            EventRow(event: event)
        }
        .listStyle(.plain)
    }
}
```

- [ ] **Step 2: Build smoke + commit**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -5
git add IrisApp/Iris/LogsTab.swift
git commit -m "feat(phase-6.1b): LogsTab with live list + filters + pause toggle"
```

---

## Task 21 : `SecurityTab` + badge `unreadAlertCount` sur status item

**Files:**
- Modify: `IrisApp/Iris/SecurityTab.swift`
- Modify: `IrisApp/Iris/AppDelegate.swift` (badge binding)

- [ ] **Step 1: Implémenter `SecurityTab`**

```swift
import IrisAppCore
import IrisKit
import SwiftUI

struct SecurityTab: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        VStack(spacing: 0) {
            HStack {
                Text("\(model.unreadAlertCount) unread")
                    .font(.callout)
                    .foregroundStyle(model.unreadAlertCount > 0 ? Color.red : Color.secondary)
                Spacer()
                Button("Mark all read") { model.markAllAlertsRead() }
                    .disabled(model.unreadAlertCount == 0)
            }
            .padding(.horizontal, 12)
            .padding(.vertical, 6)
            Divider()
            if model.alerts.isEmpty {
                Spacer()
                Text("No alerts.").foregroundStyle(.secondary)
                Spacer()
            } else {
                List(sortedAlerts) { event in
                    AlertRow(event: event, focused: event.id == model.focusedAlertID)
                }
                .listStyle(.plain)
            }
        }
    }

    private var sortedAlerts: [Event] {
        model.alerts.sorted { lhs, rhs in
            if let l = lhs.alert, let r = rhs.alert, l.severity != r.severity {
                return l.severity > r.severity
            }
            return lhs.timestamp > rhs.timestamp
        }
    }
}

private struct AlertRow: View {
    let event: Event
    let focused: Bool

    var body: some View {
        VStack(alignment: .leading, spacing: 4) {
            HStack {
                severityChip
                Text(event.alert?.secretName ?? "").font(.callout.bold())
                Text("→").foregroundStyle(.secondary)
                Text(event.host).font(.callout)
                Spacer()
                Text(event.timestamp.formatted(date: .omitted, time: .standard))
                    .font(.caption.monospacedDigit()).foregroundStyle(.secondary)
            }
            if let snippet = event.alert?.snippet {
                Text(snippet).font(.callout.monospaced()).foregroundStyle(.secondary)
                    .lineLimit(2)
            }
        }
        .padding(.vertical, 4)
        .background(focused ? Color.accentColor.opacity(0.15) : Color.clear)
    }

    private var severityChip: some View {
        let severity = event.alert?.severity ?? .low
        return Text(severity.rawValue.uppercased()).font(.caption.bold())
            .padding(.horizontal, 6).padding(.vertical, 2)
            .background(color(for: severity).opacity(0.2))
            .foregroundStyle(color(for: severity))
            .clipShape(Capsule())
    }

    private func color(for severity: Alert.Severity) -> Color {
        switch severity {
        case .high: return .red
        case .medium: return .orange
        case .low: return .yellow
        }
    }
}
```

- [ ] **Step 2: Badge binding + icon "active" pulse animation dans `AppDelegate`**

Ajouter dans `applicationDidFinishLaunching`, juste après le setup `statusItem` :

```swift
        // Bind badge count to unreadAlertCount via Combine.
        appModel.$unreadAlertCount
            .receive(on: DispatchQueue.main)
            .sink { [weak self] count in self?.updateBadge(count) }
            .store(in: &cancellables)

        // SPECS §15.1 — "Active" subtle pulse on the icon after each substituted event.
        // Triggers a 5s 0.6→1.0 alpha pulse when a new .substituted event appears.
        appModel.$events
            .compactMap { $0.first }
            .removeDuplicates(by: { $0.id == $1.id })
            .filter { $0.kind == .substituted }
            .receive(on: DispatchQueue.main)
            .sink { [weak self] _ in self?.pulseIcon() }
            .store(in: &cancellables)
```

Ajouter en haut de classe :

```swift
    private var cancellables: Set<AnyCancellable> = []
    private var pulseWorkItem: DispatchWorkItem?
```

Et les méthodes :

```swift
    private func updateBadge(_ count: Int) {
        guard let button = statusItem.button else { return }
        if count == 0 {
            button.title = ""
        } else {
            button.title = " \(count)"
        }
    }

    private func pulseIcon() {
        guard let button = statusItem.button else { return }
        pulseWorkItem?.cancel()
        button.alphaValue = 0.6
        NSAnimationContext.runAnimationGroup { ctx in
            ctx.duration = 0.4
            button.animator().alphaValue = 1.0
        }
        // Re-arm after 5s to allow the next pulse to feel fresh.
        let work = DispatchWorkItem { [weak button] in button?.alphaValue = 1.0 }
        pulseWorkItem = work
        DispatchQueue.main.asyncAfter(deadline: .now() + 5.0, execute: work)
    }
```

L'import `import Combine` doit être déjà présent (ajouté Task 18). Si non, l'ajouter.

- [ ] **Step 3: Build smoke + commit**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -5
git add IrisApp/Iris
git commit -m "feat(phase-6.1b): SecurityTab + unreadAlertCount badge + icon pulse on substitution"
```

---

## Task 22 : Banner "daemon down" + "notifications disabled" dans `PopoverView`

**Files:**
- Modify: `IrisApp/Iris/PopoverView.swift`

- [ ] **Step 1: Ajouter une bannière au-dessus du segmented control**

Modifier le `body` de `PopoverView` pour insérer une bannière conditionnelle entre `HeaderBar` et le `Picker` :

```swift
            HeaderBar()
                .padding(.horizontal, 12).padding(.vertical, 8)
                .background(.bar)
            Divider()
            BannersStack()
            Picker("Tab", selection: $model.selectedTab) {
```

Et ajouter la struct dans le même fichier :

```swift
private struct BannersStack: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        VStack(spacing: 0) {
            if case .down(let reason) = model.daemonStatus {
                banner(
                    text: reasonText(reason),
                    color: .red,
                    cta: "Retry",
                    action: {}  // No-op: SyncCoordinator polls automatically every backoff tick.
                )
            }
            if !model.notificationsEnabled {
                banner(
                    text: "Notifications disabled. Enable in System Settings → Notifications → Iris.",
                    color: .orange,
                    cta: nil,
                    action: nil
                )
            }
        }
    }

    private func reasonText(_ reason: DaemonStatus.DownReason) -> String {
        switch reason {
        case .notRunning: return "Daemon stopped. Restart via launchctl or relaunch Iris."
        case .rpcError(let msg): return "Daemon unreachable: \(msg)"
        }
    }

    @ViewBuilder
    private func banner(
        text: String,
        color: Color,
        cta: String?,
        action: (() -> Void)?
    ) -> some View {
        HStack {
            Text(text).font(.callout)
            Spacer()
            if let cta, let action {
                Button(cta, action: action).buttonStyle(.bordered)
            }
        }
        .padding(.horizontal, 12).padding(.vertical, 6)
        .background(color.opacity(0.15))
    }
}
```

Note : le bouton "Retry" est un no-op visuel parce que le `SyncCoordinator` gère déjà la reconnexion automatique en backoff. On le garde pour que l'user voie qu'il y a une action en cours. Une future itération pourrait wire `coord.bootstrap()` ici.

- [ ] **Step 2: Build smoke + commit**

```bash
xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris -destination "generic/platform=macOS" build 2>&1 | tail -5
git add IrisApp/Iris/PopoverView.swift
git commit -m "feat(phase-6.1b): banners for daemon down + notifications disabled states"
```

---

## Task 23 : `swift-format` lint étendu à `IrisApp/` + CI workflow update

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: Inspecter le workflow existant**

```bash
cat .github/workflows/ci.yml
```

Identifier les steps `swift-format lint`, `swift build`, `swift test`. Noter l'ordre.

- [ ] **Step 2: Étendre le lint à `IrisApp/`**

Dans le step `swift-format lint`, changer :

```yaml
        run: swift-format lint --strict --recursive Sources Tests
```

en :

```yaml
        run: swift-format lint --strict --recursive Sources Tests IrisApp
```

- [ ] **Step 3: Ajouter step `xcodebuild` (continue-on-error en 6.1)**

Après le step `swift test`, ajouter :

```yaml
      - name: Build IrisApp via xcodebuild
        continue-on-error: true
        run: |
          xcodebuild \
            -project IrisApp/Iris.xcodeproj \
            -scheme Iris \
            -destination "generic/platform=macOS" \
            -configuration Debug \
            build
```

- [ ] **Step 4: Vérifier local que `swift-format lint` passe sur IrisApp/**

```bash
swift-format lint --strict --recursive IrisApp/Iris
```

Si erreurs : fixer inline (mêmes patterns que IrisAppCore). Commit `style: ...` si nécessaire.

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci(phase-6.1b): extend swift-format lint to IrisApp + add xcodebuild step (continue-on-error)"
```

---

## Task 24 : Push branch + open PR 6.1b avec smoke checklist exhaustive

**Files:** none

- [ ] **Step 1: Push**

```bash
git push -u origin feat/phase-6.1b-menubar-ui
```

- [ ] **Step 2: Open PR**

```bash
gh pr create --title "feat(phase-6.1b): Iris.app menu-bar read-only (Overview/Logs/Security + notifs)" --body "$(cat <<'EOF'
## Summary

Phase 6.1b : `Iris.app` menu-bar accessory app consommant `IrisAppCore` (PR 6.1a). Livre :

- Xcode project `IrisApp/Iris.xcodeproj` (bundle `io.iris.app`, `LSUIElement=YES`, `NSAllowsLocalNetworking=YES`, ad-hoc sign).
- `AppDelegate` AppKit : `NSStatusItem` (template icon + badge count) + `NSPopover` + right-click Quit menu + multi-instance protection.
- `NotificationCoordinator` : permission request, click routing vers Security tab + focusedAlertID.
- 3 onglets SwiftUI read-only : Overview (counters + last 5 events), Logs (live + filtres + pause), Security (alerts sorted by severity + Mark all read).
- Bannières daemon down / notifications disabled.
- Wiring SyncCoordinator (bootstrap + SSE reconnect + stats poll) sur les ports/socket par défaut.

CI : `swift-format lint` étendu à `IrisApp/`, step `xcodebuild` ajouté (continue-on-error en 6.1, à promouvoir obligatoire en 6.2).

## Spec source

`docs/superpowers/specs/2026-05-28-phase-6.1-menubar-readonly-design.md`

## Smoke testing

Lancer `irisd` localement, puis lancer `Iris.app` depuis Xcode :

- [ ] App se lance, icône clé apparaît dans la status bar (template monochrome).
- [ ] Daemon down (kill irisd) → icône passe sans badge, popover montre bannière "Daemon stopped".
- [ ] Daemon up : badge vert, uptime correct ; faire `curl --proxy http://127.0.0.1:8888 https://api.anthropic.com/` → `req_total` incrémente dans Overview en ≤ 5 s ; l'icône pulse brièvement (alpha 0.6 → 1.0) après chaque substitution.
- [ ] Onglet Logs : un curl substitué apparaît en temps réel (≤ 500 ms), colonnes correctes.
- [ ] Filtres Logs : kinds multi-select, host substring, search box, fonctionnent en AND.
- [ ] Pause toggle Logs : snapshot fige la liste, untoggle restaure le live.
- [ ] Provoquer un `exfilBlocked` (config secret avec allowed_hosts restrictif + requête vers host non autorisé) → event apparaît dans Security, badge rouge avec count +1 sur l'icône.
- [ ] Notification système : pour severity ≥ medium, une notif macOS apparaît ; click sur la notif → popover s'ouvre sur onglet Security focused sur l'event (background bleu sur la row).
- [ ] "Mark all read" reset le badge à 0. Quitter (right-click → Quit) et relancer → badge reste à 0 (acknowledgment persisté UserDefaults).
- [ ] `iris pause` via CLI → popover header passe à "Paused" + bouton Resume dans ≤ 5 s.
- [ ] Lancer une 2e instance de Iris.app (cmd+space → Iris) → la nouvelle quitte immédiatement (multi-instance protection).
- [ ] Notifications denied dans System Settings → bannière apparaît dans Security tab, badge continue de fonctionner.
- [ ] Right-click icône → menu "Quit Iris" → app quitte proprement.
- [ ] Kill irisd à chaud puis restart → icône passe rouge en ≤ 30 s, popover montre bannière retry, après restart les events depuis `since:` sont rejoués.

## Quality gates

- [ ] `swift build -c release` clean.
- [ ] `swift test` passe (incl. IrisAppCoreTests).
- [ ] `swift-format lint --strict --recursive Sources Tests IrisApp` clean.
- [ ] `xcodebuild -project IrisApp/Iris.xcodeproj -scheme Iris build -destination "generic/platform=macOS"` réussit.
EOF
)"
```

- [ ] **Step 3: Trigger Gemini polling** (per CLAUDE.md §8 pattern, voir Task 13 référence).

---

# Notes de risque pour l'exécuteur

1. **Task 14 nécessite Xcode UI** — pas d'exécution agentique possible pour bootstrap du `.xcodeproj`. Bien suivre les instructions étape par étape avec l'utilisateur. Une fois bootstrappé et committé, le reste est entièrement automatisable.

2. **SwiftPM local package dans Xcode** (Task 14 step 13) — la première fois que l'on ajoute `..` comme local package, Xcode peut afficher des warnings sur les targets. Sélectionner `IrisAppCore` ET `IrisKit` à ajouter au target Iris (pas seulement IrisAppCore). Sans IrisKit explicite, certains types (`Event`, `Alert`) seraient inaccessibles aux Views même si IrisAppCore les ré-exporte transitivement.

3. **`@MainActor` + `Sendable` strict concurrency** (Tasks 6-10) — les fakes sont `@unchecked Sendable` parce qu'elles mutent des champs depuis le main actor des tests. C'est cohérent avec le pattern XCTest. Le linter ne flag pas `@unchecked Sendable` sur les types de tests.

4. **`button.title` pour badge** (Task 21) — c'est la solution la plus simple pour macOS 13. Pour un vrai cercle rouge custom, il faudrait un `NSView` overlay sur le button — différé Phase 6.x si le rendu textuel est jugé insuffisant.

5. **`button.image?.isTemplate = true`** (Task 16) — sans cette ligne, l'icône ne respecte pas le mode dark/light de macOS. C'est la convention pour les NSStatusItem.

6. **`Settings { EmptyView() }`** (Task 16) — SwiftUI App requiert au moins une Scene. `Settings` est invisible avec `LSUIElement=YES` et ne pollue pas le menu. C'est un pattern standard menu-bar SwiftUI.

7. **Gemini review** : suivre le pattern CLAUDE.md §8 — 1 fix par commentaire OU refuse factuel avec citation. Polling `/loop 30s` avec auto-stop sur 10 min de silence consécutif, plafond 30 min.

8. **Si une task échoue au build après modif Xcode-pbxproj** : ne pas paniquer. Ouvrir Xcode, regarder l'erreur (les messages clang/swift sont plus lisibles dans Xcode), corriger, re-build CLI, commit.
